# slice-S3 fix dispositions

Run `20260912-042809-fix-slice-S3`. One reviewer (codex), so the `/name` suffix is
omitted. Every finding was reproduced or refuted against the tree before it was acted on
(GP1); the reproduction is recorded in each row.

| F1 | verified: yes | action: fixed | reproduced: deleted `("key-type", "Key types")` from renderers.py:121, gate exited 0 with 22 checks green — the claims fell to "Other claims" and line 794 filtered key-type out of the heading list. Sharpened test-structural-claims.mjs to resolve all five categories through a new `claimGroup()` that reads the rows beneath `### <heading>`; the same sabotage now reds ("the page does not group the claims under Key types"), and the tree greens with it reverted. 16148e6 |
| F2 | verified: yes | action: fixed | reproduced: inverted phase-4-adversarial.md:183 to "may advance whether or not…"; gate exited 0, because the assertion ended in a bare `\|mapped/i` that the document's other two mentions of the word satisfy. Sharpened to read the clause's direction — sentences carrying both `mapped` and a claim outcome must state an obligation and must not state a permission. Sabotage reds, revert greens. Kept in prose, not in `enforcePhasePrerequisites`: `claim_validity_events.event_type` (schema.sql:1060) has no `survived` value, so no query separates a survived claim from an unchallenged one and a substrate gate would refuse the all-survived result phase-4-adversarial.md's Rules call legitimate. 16148e6 |
| F3 | verified: yes | action: fixed | reproduced: `upsert_subsystem` (subsystems.ts:109) calls only `enforceMonotonicTransition`; a runtime probe carried a scoped, claimless B-20 to `structural`, and opened B-21 straight at `structural` with no ledger at all. Extracted `enforceForwardPrerequisites` into invariants.ts and called it from both status writers, reading a fresh insert as a transition from `unmapped`. red c765955, green 509065e |
| F4 | verified: no | action: rejected | not reproduced as a claim-unmet. C42 requires refusal when evidence "cites a different file than `subject_id`", and §9.1's normative check 2 (spec.md:1114) names `evidence.file_path` alone; claims.ts:76 implements exactly that. The reviewer read the §9.1 table's "Required evidence" column, which the paragraph beneath it calls prose the checks do not implement. The tension between table and check is real and worth a spec edit, but `evidence.symbol` is a field the writer authors, and forcing a code match on it is the generative-field case GP8's v2 scope note excludes from substrate enforcement. Tightening past C42 would reopen a recorded decision, not repair a defect. |
| F5 | verified: yes | action: fixed | reproduced: a runtime probe superseded `B-09/key-type/probe-refuse` with a lone `name-inferred` row on another file and the successor became current — `supersede_claim` ran `requireEvidence` but not `requireFileAnchoredEvidence`, while the successor inherits the predecessor's `claim_key` and `subject_id`. Moved the check into `insertClaim`, the one function that writes a `claims` row, and judged the successor on the rows that will actually support it rather than on ids carried over from the predecessor. §9.1's concurrency and seam exemption rides along in the claim_key pattern, asserted separately. red c765955, green 509065e |
| F6 | verified: yes | action: fixed | reproduced: after `reset_subsystem(B-23, to_status: "scoping")` one current `B-23/` claim remained, so the re-advance was satisfied by the reading the reset had just discarded. `reset_subsystem` now discards the subsystem's `<sid>/` claims when it regresses below `structural`, beside the ledger it already cleared, and reports the count; supersession rows go first (ON DELETE RESTRICT), claim_evidence and claim_validity_events cascade. A reset that stops *at* `structural` keeps them, asserted. red c765955, green 509065e |
| F7 | verified: yes | action: deferred (P14) | reproduced: locus.ts:1443 attaches `view.narrative` on `if (build.narrative)`, with no dependence on `requested`, and describe_locus's input schema has no narrative opt-in — §4.2 lists it as opt-in and §5.1 fixes both the signature and the `sections` enum, so no channel exists. The drift is real, but P11 resolved it the other way on purpose and recorded the resolution in three places: locus-account.schema.json:475 documents the narrative as a pointer distinct from the page's prose, and test-structural-claims.mjs:654-669 asserts that pointer positively. Closing it here means deleting those assertions or widening §5.1's input surface. Deferred to P14, the packet that reopens describe_locus's input surface; the reason is recorded in P14's `notes` so the decision is made where §4.2, §5.1 and §9.1's "on the page only" can be reconciled together. |
| F8 | verified: yes | action: fixed | reproduced: `get_claims`'s input schema (claims.ts:511) carries no subsystem or prefix filter and sets `additionalProperties: false`, and phase-4-adversarial.md:45 said so and fell back to a client-side filter — making the completeness of the adversarial pass a property of the caller's code. Added `subsystem_id`, matched with `substr` not `LIKE` so a `_` or `%` in an id cannot reach another namespace, and pointed the instruction at it. Gated in test-phase2-claims-gate.mjs on both the schema and a runtime call that must return B-30's claim and not B-30x's. |
| F9 | verified: yes | action: fixed | reproduced: phase-2-structural.md:78 defined `<symbol-slug>` as the symbol alone, and schema.sql:1042 admits one current row per `claim_key`, so `src/http/config.ts:Config` and `src/db/config.ts:Config` both slug to `<sid>/key-type/config` and the second reading is refused. §9.1 writes `<symbol-slug>` without defining it, so the definition — not the spec's key shape — was the defect: it is now the whole `<path>:<symbol>` pair. Gated on the definition (bounded to its own paragraph, since the bullets below carry `<path>` regardless) and on both readings staying current; the prior wording reds it. |
| F10 | verified: yes | action: fixed | reproduced exactly: `_nest_prose("~~~text\\n# literal code heading\\n~~~", 3)` returned `~~~text\\n#### literal code heading\\n~~~`. `renderers.py` toggled on ` ``` ` alone, so a tilde fence was invisible and its contents were treated as prose — bytes the label calls verbatim. Replaced with a CommonMark fence tracker: either character, three or more, closing only on the same character at least as long. Five cases asserted in materializer/test-materializer.py, including a heading outside every fence that must still move. |

## Notes

- Both packet gates are green, and all 19 unique regression commands of P11 and P12 pass.
- Every sabotage applied during verification was reverted; `git status` is clean at the
  final commit.
- Nothing was weakened. Each repair that removed a hole added the accepting case beside
  it (VP4): a claimed subsystem must still upsert to `structural`, a sound supersession
  and a derived concurrency supersession must still be accepted, a reset that stops at
  `structural` must keep its claims, and a heading outside every fence must still move.
