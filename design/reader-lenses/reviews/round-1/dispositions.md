# Round-1 dispositions

One row per aggregate item. `verified` records whether the reviewer's evidence held when this
session checked it against the repository, the schema, the AxiomDB store (read-only), or the
cited document. `applied-in-part` means the defect was verified and repaired, but by a different
change than the reviewer proposed; the note says which.

Aggregate: `design/reader-lenses/reviews/round-1/aggregate.md`. Run `20260911-195201-revise`.

| C1/codex | verified: no | action: rejected | read spec.md 6.1 History membership against the schema: its event spine is finding_resolution_events and contradiction_resolution_events, both tables with no UPDATE or DELETE anywhere in mcp-server/src or materializer, plus sessions, refresh_runs and projection_verification_runs; finding_resolution_current (schema.sql:852-861) is a view over the events. The cited mutable rows (dispositions, seams, vocabulary, subsystems) are Codebase-lens sources and the History lens never asserts a historical reading of them. The evidence offered bears on 3.3's as_of_sha whole-account snapshot, which is C16 and is overturned and repaired. Decision 2's fallback is therefore not triggered; the narrow part that does hold (open_questions and field_notes have no event table) is applied as 1.1's honesty paragraph and new claim C58 |
| C2/codex | verified: yes | action: applied | confirmed against schema.sql: dispositions 198-221 and open_questions 4538-4560 are outside 6.1's partition queries. Applied the exhaustive-census obligation to P2 acceptance rather than a new record-family taxonomy: every findings row must land in exactly one lens membership query and a row in both or neither turns the gate red. Terminal concern dispositions stay on the Method coverage page, which is what the Method lens is for; 1.1 now states which families carry an event trail and which do not |
| C2/claude | verified: yes | action: applied | store confirms 166 disposition rows: confirmed-acceptable 86, confirmed-bug 25, out-of-scope 21, ruled-out 34, unresolved-competition 0. 7.1 gave no Unresolved or History page to disposition rows. Applied as stated: terminal dispositions stay on concerns.md under Method, and 6.1 gains a resolved-leads.md History page for the two families (questions, notes) that did fall through |
| C4/claude | verified: yes | action: applied | reproduced both facts read-only: the vocabulary table holds the term Severity (sh:Violation / sh:Warning / sh:Info), which the old order kinds symbol; 65 of 209 evidence.symbol values contain a double colon (the review said 71 - the substance holds, the count was off). Spec 2.1 now states a six-step order with exact vocabulary.term before the colon test, an explicit first-colon split, and a path-shaped step so a never-reconciled path resolves unledgered rather than not-defined. Claim C4 rewritten; P5 acceptance asserts the corrected order on all six cases |
| C5/claude | verified: yes | action: applied | confirmed db.py:15-23 opens mode=ro and applies no schema, and db.ts:48-53 initializeSchema is called only by openDatabase. Spec 2.2 now requires the materializer to probe sqlite_master for both views and turn the publish red with a named cause; the inline-fallback alternative is explicitly refused as a second copy of one definition. New claim C56; P5 deliverables gain core.py and db.py |
| C6/codex | verified: yes | action: applied | ADR-0001 section Current makes a resolving evidence revision part of current authority, and claims.ts:64-72 resolveCommit throws rather than returning unchecked. Applied as a conservative authority downgrade rather than a new headline state: with reachability_checked false the state stays examined but the served authorizes and cannot_justify text is the examined-stale row, flagged authorization_downgraded |
| C6/claude | verified: yes | action: applied | confirmed git.ts:246-262 pushes a row to unverifiable only when git diff --name-only fails, and git.ts:274-275 writes that reason; all 4 distinct examined ref_sha on the store resolve and are ancestors, so the store cannot exercise the branch. Applied the distinct-reason remedy: unreachable-ref for a resolvable non-ancestor, unverifiable-ref kept with detect_changes's meaning. New claim C57 |
| C8/codex | verified: yes | action: applied | schema.sql:176 PRIMARY KEY (subsystem_id, file_path) confirmed, and the store's crates/axiomdb-server/src/main.rs carries 10 owners. Applied as a deterministic overflow response: when the complete owners array alone would exceed the 32768 hard ceiling the tool errors with path and owner count and names list_scope as the route, because a partial array makes the mixed headline undecidable |
| C9/codex | verified: yes | action: applied | invariants.ts:23-33 states deferred is not on the axis and STATUS_ORDER excludes it, while schema.sql:627-628 includes it in the CHECK. Applied: the ceiling ranks non-deferred owners only, deferred owners are listed in deferred_owners with their reason, and an all-deferred file has ceiling deferred authorizing nothing |
| C11/codex | verified: yes | action: applied | scope_gaps records only unledgered and absent paths (schema.sql:186-193) and is rebuilt each run, so a ledgered path has zero gap rows whether or not reconciliation ever ran. Narrowed to the exact condition the rows prove: ledger_reconciled is non-null only at owner_rows = 0, and a reconciliation receipt from git_state.last_checked_sha is reported otherwise |
| C12/codex | verified: yes | action: applied | reproduced the join read-only: 1094 (subsystem, active concern) pairs lack a disposition, 0 of 36 active concern codes lack one everywhere, 9 of 20 seams lack an SC-prefixed disposition on exactly one side, 0 lack it on both. 2.4.6's unassessed-seam was quantified over either party and reported zero. Both predicates re-based on the pair: (owner, concern) and (seam, side) |
| C12/claude | verified: yes | action: applied | confirmed dispositions PK is (subsystem_id, concern_code) at schema.sql:220 with no seam id, 6 SC-prefixed concerns and 19 SC-prefixed dispositions over 20 seams, composition_seam_concerns empty, and 0 of 103 field_notes.location values equal a subsystem id with 6 carrying a citation. Applied both remedies: every unassessed-seam entry carries binding per-party-proxy, and open-lead matches comma-split path prefixes and reports location_match exact, prefix, owner or none |
| C13/codex | verified: yes | action: applied | describe_locus's signature in 5.1 takes no subsystem argument and a not-defined term has no vocabulary row to carry one, so the same-subsystem tier had no referent. Tier removed; the order is prefix then lexicographic, stated as globally deterministic |
| C13/claude | verified: yes | action: applied | same removal, plus the symbol half: 45 of 209 evidence.symbol values carry a parenthetical qualifier such as GraphWriteCoordinator::execute_inner (SSI retry fence re-check), so exact match under-reports. 2.5 now reports symbol_prefix_matches separately, never merged into symbol_cited |
| C16/codex | verified: yes | action: applied | confirmed the 3.3 predicate drops open claims opened at an ancestor, and that dispositions, seams, vocabulary, subsystems and file_ledger carry no validity interval or event table. Applied the stronger remedy: whole-account historical snapshots are removed. Only claims, findings and the event tables support as_of_sha; every other section is served at current and marked as_of_supported false with a sentence saying so |
| C16/claude | verified: yes | action: applied | confirmed claims.ts:484-486 selects every row when query_sha is set and filters with claimAppliesAt at claims.ts:156-159. The 3.3 pre-filter SQL is deleted and the candidate set is every claims row, ancestry deciding membership |
| C17/codex | verified: yes | action: applied | 3.4 said no field may contain text a model produced during the call, which is true and narrower than it reads. Restated as text generated during this tool call, and every item now carries authored model or code with the durable model-authored fields enumerated. New claim C59 |
| C18/codex | verified: yes | action: applied | confirmed helpers.ts:43-56: jsonResult emits JSON.stringify(data, null, 2) as the text block and repeats the object in structuredContent. Budgets now measure the whole wire response including structuredContent, and the three tools take a new compact serialization path. structuredContent is kept rather than dropped because the MCP contract and existing hosts depend on it; it is counted, not removed |
| C18/claude | verified: yes | action: applied | same evidence, same repair. The gate also prints the default pretty size beside the compact one so the overhead the budget accounts for is visible rather than assumed |
| C19/codex | verified: no | action: rejected | the default and opt-in section split survives on its own account, and the rest of the item is conditional on decision 2's fallback, which C1/codex does not trigger. The History-lens pointer stays; 3.1's history_pointer keeps counts on by default and detail opt-in |
| C20/codex | verified: yes | action: applied | confirmed ADR-0011:63-65 names policy, irrelevant and budget, and that 4.3 requires every omitted id to be present in the census. A section whose table holds no row has census 0 and no id, so not-recorded could not satisfy the invariant it sat inside. Removed; an unrecorded source is census 0 with recorded false. irrelevant is also not carried, and 4.3 says why: selection here is registry-exact, not lexical |
| C20/claude | verified: yes | action: applied | same evidence and the same remedy, stated as a per-section boolean rather than an omission row |
| C23/codex | verified: yes | action: applied | confirmed ADR-0010:42-43 defines contested as part of the terminal A7 challenge aggregation. The label is renamed undiscriminated, the page becomes disagreements.md, and the additional source distinctions are carried as fields rather than by overloading a label. This is the only claim both reviewers overturned |
| C23/claude | verified: yes | action: applied | the same rename, plus the second half: review-session.ts:222-227 computes latent-defect with isAncestor(finding.ref_sha, impact.base_sha) and get_attention has no composition. latent-defect is dropped rather than re-based, because substituting a different base under the same word is the defect being repaired; the alternative pre-checkpoint-open label was not adopted (GP37) |
| C24/codex | verified: yes | action: applied-in-part | confirmed field_notes has no resolution event and no resolved time (schema.sql:291-302) and sessions has no locus relation (schema.sql:598-604). The schema additions were not made: open_questions does carry resolved_at (schema.sql:4559) and sessions are attributable through citing rows, so the promised categories are kept with their ordering basis stated rather than removed or backed by new tables (GP37) |
| C24/claude | verified: yes | action: applied | confirmed file_ledger has no session_id, so session attribution runs through citing rows only; store holds 24 sessions and 209 evidence rows over 71 of 295 ledger paths. The response now declares session_attribution by-citation, and C24 states the two ordering bases explicitly |
| C27/codex | verified: yes | action: applied | verified each citation: renderers.py:446-451 and findings.ts:402-406 carry full inline fallbacks and findings.ts:440-443 a partial one. review.ts:394-418 does not carry a fallback - it reads finding_resolution_current directly, as review-session.ts:190-197 does - so the defect there is the opposite one, under-reporting legacy rows. All five readers are listed in spec 6 and added to P2 deliverables, with a static check for any remaining fallback expression |
| C27/claude | verified: yes | action: applied | same migration, and the materializer view probe from C5 is required in the same packet |
| C28/codex | verified: yes | action: applied | confirmed 7.1 defines Method by a fixed page list. Weakened to what is true and mechanically checkable: Method membership is a fixed page list, and the gate checks group membership against NAV_GROUPS rather than asserting a SQL predicate that does not exist |
| C28/claude | verified: yes | action: applied | reproduced all three: 111 of 111 candidate rows carry a 40-character ref_sha so drifted candidates would render under Examined files the repository has changed under; diagnosticity_sessions.outcome admits unresolved-competition (schema.sql:4478-4479) and fell in neither lens; open_questions has 5 answered rows and field_notes 103 rows with no History page. All three applied - predicate narrowed to classification examined with drifted candidates reported separately, unresolved-competition matrices made Unresolved, and resolved-leads.md added. New claims C60 and C61 |
| C29/claude | verified: yes | action: applied | confirmed core.py:455-462 routes every finding id to findings.md and readback.py:268 reports cross-link-anchor. _build_xref_index now routes by finding_state_current.resolution_state; stated in 6.2 and in P2 acceptance |
| C31/codex | verified: no | action: rejected | conditional on decision 2's fallback, which C1/codex does not trigger. NAV_GROUPS keeps History; no regrouping is applied |
| C32/codex | verified: yes | action: applied | first viewport is not decidable by any gate: it varies with viewport size, zoom, font metrics and enlarged text, and test-lens-pages.py could not evaluate it. 7.2 is re-titled source order and now contracts over source order plus a render at 360, 768 and 1280 CSS pixels, with the literal viewport guarantee withdrawn |
| C33/codex | verified: yes | action: applied | confirmed schema.sql:630 documents scope as free-text key files, directories, symbols, and the store's values are path and boundary lists (W-01 begins crates/axiomdb-core/src/write/coordinator.rs (4008 lines)). scope now renders under a Scope heading, never as a purpose sentence; no durable purpose field is invented, and a subsystem without one says so |
| C34/codex | verified: yes | action: applied | confirmed slugs.py:20-23 collapses every non-alphanumeric run to a single dash, so src/a/b.ts and src/a-b.ts collide. The Files anchor becomes f- plus the first 10 hex of sha1(path), and the examined revision renders per owner - the store holds 53 multi-owner paths and one with ten owners, so a singular revision would assert an agreement the ledger does not record |
| C35/codex | verified: yes | action: applied | same query as C12: the global predicates report zero on a store holding 1094 undispositioned pairs and 9 one-sided seams. 7.5 now has five sections each counted over the unit its gap occupies, with matching denominators, and P8 acceptance requires a partial-coverage fixture to report non-zero in both |
| C35/claude | verified: yes | action: applied | the numerator cannot bind to a seam - dispositions carry no seam id and composition_seam_concerns is empty - so the unit is the (seam, side) pair and every entry carries the per-party-proxy sentence. assessable=0 is kept as the other disjunct |
| C37/codex | verified: yes | action: applied | confirmed schema.sql:813 requires evidence_id only for verified-fixed. Applied as a labelled basis rather than a new evidence requirement, because ADR-0001 section Resolved licenses an explicit authorized dismissal: basis.kind is evidence, authorized-dismissal, or none-recorded, a none-recorded row is counted at the top of the page, and the 7.1 hint no longer says proof |
| C39/codex | verified: yes | action: applied | the cited lines 690-708 are CSS; the inline JS is the _JS block at html_projection.py:877 with setMenuOpen at 916, and it does implement theme persistence and mobile navigation. The substance holds: 8.1 is re-scoped to new behaviour and names the existing shell functions as preserved and out of scope |
| C41/claude | verified: yes | action: applied | confirmed readback.py:147-152 globs only markdown and html, core.py:285 builds expected_paths from alive plus html files, and core.py:313-327 verify_projection derives them from the plan alone. Both changes applied to 8.2 and to P10 acceptance, which now requires a green publish with the file present and a red coverage axis when it is removed |
| C42/codex | verified: yes | action: applied | confirmed add_claim validates neither evidence kind nor subject correspondence: claims.ts:98-115 requireEvidence checks reachability only, claims.ts:233 declares subject_type as a bare string, schema.sql:943 and 957 add no CHECK beyond length. Applied to the three structural categories only; concurrency and seam claims keep the weaker requirement, because forcing a kind a derived claim cannot honestly carry invites fabrication to order (BP4) |
| C42/claude | verified: yes | action: applied | same evidence, and the store confirms claims and claim_evidence are both empty so neither check has real data to validate against - test-structural-claims.mjs seeds both the accepted and the refused case. Substrate check added to add_claim, subject_type validated against the 10.1 enum |
| C43/codex | verified: yes | action: applied | confirmed phase-4-adversarial.md section Process step 1 pulls confirmed bugs and linchpin dispositions and never reads claims. The phase gains current claims for the subsystem as targets and records each challenge outcome before mapped; phase-4-adversarial.md added to P12 deliverables |
| C43/claude | verified: yes | action: applied | confirmed test-perf-ceilings.mjs:92 runs git init with no commit and advances to structural at 109, and test-perf-tier2.mjs:66-72 and test-cloud-e2e.mjs:140,256 do the same; add_claim's resolveCommit cannot resolve a ref_sha in a repo with no commits. All three fixtures added to P12 deliverables and two to its regression list |
| C45/codex | verified: yes | action: applied | reproduced the parse by reading helpers.ts:99-125 and 79-96: separator is indexOf of a colon and revision is lastIndexOf of an at-sign over the whole string, and requireWorkspaceSourcePath accepts spaces, so a prose prefix ending in a colon is normalized as the path and the real citation is never checked. Nothing resolves the revision. Applied: token-level extraction against a stated grammar, path normalization and git rev-parse on the token. Symbol reachability is not required and 9.2 says why - it needs a parser the server does not have |
| C45/claude | verified: yes | action: applied | the complementary direction: a prose context with no at-sign is rejected outright though xrefs.context is documented as one-line prose (schema.sql:86). Token extraction fixes both directions and the prose is stored verbatim. test-smoke.mjs:254 confirmed to call add_xref with no context and is added to P13 deliverables |
| C48/codex | verified: yes | action: applied | confirmed SQL CHECK enums are independent literals and that check-evidence-vocabulary.mjs:17-63 compares only evidence kinds, evidence quality and the SKILL.md ladder - 2 of the 11 enums the draft carried. Applied: the source now carries every CHECK-constrained vocabulary, gen-vocabulary.mjs --check-sql asserts parity against schema.sql without generating it, and P1 deliverables gain schema.sql and the enforcing validators |
| C48/claude | verified: yes | action: applied | confirmed the regex at check-evidence-vocabulary.mjs:17-28 reads a const KINDS literal from evidence.ts and throws when absent, and that following the import would compare an array with itself. The check is replaced by a four-way comparison over the JSON source, both generated files and SKILL.md's ladder, each read independently |
| C49/codex | verified: yes | action: applied | reproduced in Python: the word-boundary pattern matches mapped inside memory-mapped and stale inside a stale cache is invalidated by. 11.1 now matches survey-status assertions - a copula whose subject is the project, conspectus, survey or a subsystem id - excludes code spans and hyphen-prefixed terms, and the gate carries a negative corpus as well as a positive one (VP7) |
| C52/codex | verified: yes | action: applied | confirmed no whole-store reinitialization tool exists: reset_subsystem is per-subsystem and commit_phase_gate only snapshots, and the server holds an open handle for the life of the process. 12.1 becomes seven ordered steps with an explicit stop and restart and a read-back against the empty store, split across packets P16 and P17 |
| C53/codex | verified: yes | action: applied | confirmed materialize.ts:241-245 defaults output_dir to docs and resolves it through resolveStorageOutputPath, which project.ts:1003-1012 confines beneath storagePath, and that git ls-files docs returns 48 tracked files. dev/promote-docs.mjs is added as an explicit promotion step that re-reads the promoted root docs at that path before commit. New claim C62; P18 owns it |
| C54/claude | verified: yes | action: applied | confirmed the store holds 16 open findings, 31 open questions and 103 open notes, so budget truncation makes the selected set a strict subset by design. 12.4 assertion 2 is now equality over selected ids unioned with the ids omitted under reason budget, with omitted count required to reconcile so an empty id list cannot satisfy it |
| C55/codex | verified: yes | action: applied | reproduced: running the compactness gate on the baseline exits MODULE_NOT_FOUND. Every packet now carries gate.red_expect and gate.red_rejects, and the red commit must contain the gate file. plan.json records both; the launcher change that consumes them is recorded in rationale.md under Unsettled |
| C55/claude | verified: yes | action: applied | confirmed run.sh:326 accepts any non-zero exit and run.sh:324 swallows a failed build. Same remedy, with the reject pattern stated verbatim in 13 and in every packet's gate object |
| D1/codex | verified: yes | action: applied | same query as C12: the global predicates yield zero on a store with 1094 missing pairs and 9 one-sided seams. P5 acceptance now requires a partial-coverage fixture - one subsystem missing a concern another holds, one seam assessed on exactly one side - and P8 requires both to report non-zero |
| D2/codex | verified: yes | action: applied | P14's as_of SQL is replaced by the 3.3 contract: supported sections use every stored row with ancestry deciding, unsupported sections are served at current and marked as_of_supported false. No version history is added for mutable sources; the promise is narrowed instead |
| D3/codex | verified: yes | action: applied | P7 acceptance now measures the whole wire response for the default and the expanded mode and prints the pretty-serialized size beside it; jsonResult gains the compact option as a P7 deliverable. The duplicate structuredContent is counted rather than eliminated, because hosts depend on it |
| D4/codex | verified: yes | action: applied | P1 deliverables gain schema.sql, findings.ts, claims.ts, xrefs.ts, notes.ts and diagrams.py, and its acceptance gains --check-sql parity against every CHECK the source names |
| D5/codex | verified: yes | action: applied | P10 acceptance now requires browser-driven keyboard behaviour - Arrow keys, Enter, Escape with focus restoration, and the live-region announcement - and a complete no-JavaScript traversal, rather than static ARIA attributes alone |
| D6/codex | verified: yes | action: applied | P11 gains the two substrate checks (subject_type enum, evidence kind and file correspondence) and P12 gains the Phase 4 claim-review deliverable. The per-category quota is deliberately not added: 9.1 requires one claim because forcing a generative field to a shape invites fabrication (BP4) |
| D7/codex | verified: yes | action: applied | confirmed P12 and P13 both edit phase-2-structural.md while P13 depended only on P6 and P8. P13 depends_on is now P6, P8, P12 |
| D8/codex | verified: yes | action: applied | confirmed mcp-server/src/installer.ts does not exist and mcp-server/src/cli.ts does. P15 deliverables now name cli.ts; the one-call routing claim is removed from the gate and specified as a measurement in 13.2 with two arms and two runs per condition, recorded in routing-measurement.md. New claim C64 |
| D9/codex | verified: yes | action: applied | P16 is split into P16 (snapshot, stop, delete, initialize, restart, read back - 120 min), P17 (onboarding and batched subsystem survey - 240 min) and P18 (publish, promote, dogfood gate - 150 min), each with its own gate and checkpoint |
| D10/codex | verified: yes | action: applied | dev/promote-docs.mjs is a P18 deliverable, and P18 acceptance requires verify_materialized_docs to re-read the promoted root docs at that path and match the contract before commit |
| D11/codex | verified: yes | action: applied | reproduced the module-not-found red on the baseline. gate.red_expect and gate.red_rejects are now mandatory fields on all eighteen packets, and the red commit must contain the gate file |
| D12/codex | verified: yes | action: applied | P5 gains the git-unavailable, deferred-owner, unreachable-ref and ledger_reconciled assertions and P6 gains the zero-denominator and origin-disagreement axes. Historical intervals moved to P14 under the narrowed 3.3 contract |
| D13/codex | verified: yes | action: applied-in-part | the append-only schema was not added. open_questions carries resolved_at (schema.sql:4559) and sessions are reachable through citing rows, so the two categories are kept with their ordering basis and attribution declared in the response (C24, C58) rather than removed or backed by new tables |
| D14/codex | verified: yes | action: applied | P8 acceptance now seeds three colliding path slugs and requires three distinct path-hash anchors, and requires the examined revision to render per owner |
| D15/codex | verified: yes | action: applied | ran gen-tool-inventory.mjs --check on the branch baseline: tool inventory block up to date (196 tools). The spec's 195 is corrected to 196 going to 199, and both the spec and P15 acceptance now derive the expected count from the generated inventory rather than from a literal |
| D16/codex | verified: yes | action: applied | P2 acceptance gains an exhaustive census requiring every findings row to land in exactly one lens membership query, with a row in both or neither turning the gate red |
| D17/codex | verified: yes | action: applied | the not-recorded reason is removed from 4.3 and from P7 acceptance; a section with no rows is census 0 with recorded false and contributes no ledger entry |
| D18/codex | verified: yes | action: applied | P2 deliverables gain findings.ts, review-session.ts and review.ts, and acceptance gains a static check that no COALESCE fallback expression survives outside the view |
| D19/codex | verified: yes | action: applied | P9 acceptance now seeds an accepted finding with an empty rationale and requires basis none-recorded plus a count at the top of the page, rather than treating proof as structurally mandatory - ADR-0001 licenses an authorized dismissal |
| D1/claude | verified: yes | action: applied | same evidence as C55; gate.red_expect is a per-packet regex the launcher greps in the red output file, with gate.red_rejects naming the module-not-found family explicitly |
| D2/claude | verified: yes | action: applied | confirmed the CI step in .github/workflows/test.yml and the three fixtures. test-perf-ceilings.mjs, test-perf-tier2.mjs and test-cloud-e2e.mjs are P12 deliverables, the first two are in its regression list, and acceptance requires the fixtures to seed one commit, one evidence row and one claim per subsystem |
| D3/claude | verified: yes | action: applied | findings.ts and review-session.ts added to P2 deliverables, with review.ts alongside them, and acceptance requires all four plus the renderer to agree on a legacy row with no event |
| D4/claude | verified: yes | action: applied | confirmed only P1 listed test.yml and that git ls-files .amanuensis returns 0. Every packet now lists .github/workflows/test.yml with the acceptance that its gate runs in CI, and the dogfood gate reads a committed dogfood-receipt.json plus docs/.projection-contract.json and reports which mode it ran in |
| D5/claude | verified: yes | action: applied | P10 acceptance now reads: clean publish and verify_materialized_docs green with search-index.js present, coverage red when it is removed |
| D6/claude | verified: yes | action: applied | confirmed renderers.py:154 emits the entries stale markers inside render_index and that test-projection-custody.mjs seeds an entries stale row. P3 acceptance names where the markers must still be emitted and the custody test is added to its regression list |
| D7/claude | verified: yes | action: applied | P2 acceptance now requires _build_xref_index to route finding ids by finding_state_current and a resolved finding referenced from a subsystem page to resolve to resolved-findings.md with the coverage axis green |
| D8/claude | verified: yes | action: applied | test-smoke.mjs added to P13 deliverables and the citation contract is token-level, with helpers.ts added as a deliverable |
| D9/claude | verified: yes | action: applied | the old acceptance compared two copies of one CASE. Replaced with a declared fixture table of classification, stale and stale_reason to expected standing_state covering all seven states, including candidate with stale=1 and a null classification |
| D10/claude | verified: yes | action: applied | confirmed files.md's standing column reads file_standing, delivered by P5, while P8 depended only on P2 and P4. P8 depends_on is now P2, P4, P5 |
| D11/claude | verified: yes | action: applied | confirmed 5 answered questions and 103 field notes with no page, and that unresolved-competition matrices fell in neither lens. resolved-leads.md added to 7.1 and to P9 acceptance; unresolved-competition matrices added to the Unresolved queries and to disagreements.md |
| D12/claude | verified: yes | action: applied | P7's gate now measures the emitted wire response and jsonResult gains a compact path used by the three locus tools |
| D13/claude | verified: yes | action: applied | the 150-minute packet became three packets with separate gates and budgets, and the dogfood gate now reads committed artifacts so its red run in a fresh worktree is red for the right reason |
| D14/claude | verified: yes | action: applied | the undecidable red condition is replaced by per-label fixture assertions in P14 and in the 13 gate row, after C23 resolved the label collision |
| D15/claude | verified: yes | action: applied | P1 acceptance now reads: the check reads the JSON source and both generated files and turns red when any one of the four parties diverges, with SKILL.md read independently as the only hand-written party |
| D16/claude | verified: yes | action: applied | the 10.1 enum list goes from 11 to 24 entries covering every CHECK-constrained vocabulary the named surfaces read, and the obligation predicate is generated from the file_classification flag into helpers.ts and diagrams.py rather than hand-maintained in three places |
| D17/claude | verified: yes | action: applied | omitted is aggregated per section and reason with an exact count and a 1024-byte per-entry id sub-budget that sets ids_truncated, and P7 gains a 150-item census fixture |
| D18/claude | verified: yes | action: applied | the documented order is corrected first (C4) and P5 acceptance then asserts the corrected order on a term containing a colon, a Type::method symbol, a path in no table, and a bare term |
