## Findings

| Id | Kind | Claim / Location | Severity | Evidence | Remediation |
|---|---|---|---|---|---|
| F1 | gate-cannot-red | P16 / `dev/test-rebuild-readback.mjs:315` | high | I removed `await before.stop()` from `dev/rebuild-self-conspectus-store.mjs:248`; the P16 gate remained green because it reimplements the procedure in a scratch workspace instead of invoking the production driver. | Extract a shared rebuild procedure or run the production driver against a disposable workspace so removing its stop step turns the gate red. |
| F2 | gate-cannot-red | P16, P17, P19, P18 revision binding | high | P16 stayed green after its receipt SHA was replaced with forty zeroes; `dev/test-rebuild-readback.mjs:688-690` only validates syntax. In a depth-one clone matching `.github/workflows/test.yml:94`, P17, P19, and P18 stayed green with unavailable receipt revisions and after their top-level SHAs were zeroed; their ancestry checks explicitly pass shallow clones at `dev/test-rebuild-coverage.mjs:638-647`, `dev/test-rebuild-depth.mjs:330-333`, and `dev/test-reader-lenses-dogfood.mjs:260-264`. | Use `fetch-depth: 0`, resolve every receipt provenance revision, and treat unevaluable ancestry as a skipped or failed gate rather than success. |
| F3 | claim-unmet | C52, C54 / committed rebuild receipts | high | In the supplied snapshot, P17 exited 1 because `258ccd28fc199876e82655111d3e532b6757e389` and `258ccd2` do not resolve; P19 and P18 exited 1 because `0fee11b0fea10eb55d6113dd27b57ef3d41065d3` does not resolve. These are the packets’ required gates. | Regenerate the receipts against a revision included in the deliverable, or make their evidence self-contained and independently verifiable without discarded Git history. |
| F4 | regression-fail | C53 / `dev/check-living-conspectus.mjs` | medium | The listed regression command exited 1: fixture revision `b8b566f` does not resolve, followed by `baseline-report:derived` mismatch. C53 explicitly requires this checker to remain green. | Commit a self-contained historical fixture or ensure the pinned object is available in every supported checkout. |
| F5 | claim-unmet | C52 / `dev/rebuild-self-conspectus-store.mjs:96-268` | high | The driver spawns and stops only its own `before` child and probes only that PID before recursively deleting `.amanuensis`. It never detects or coordinates with another Amanuensis server that already has `memory.db` open, contradicting the requirement that nothing hold a handle across deletion. | Make every server hold a shared store lock and require the rebuild driver to acquire an exclusive lock before deletion. |
| F6 | claim-unmet | C43 / mapped transition and P19 receipt | high | `mcp-server/src/invariants.ts:199-200` imposes no prerequisite for `mapped`, so Phase 4 outcomes can be omitted. `upsert_subsystem` can also write status directly at `mcp-server/src/tools/subsystems.ts:94-138`. Meanwhile `dev/record-rebuild-depth.mjs:388-403` synthesizes every ladder rung and hardcodes `tool: "update_subsystem_status"` rather than reading transition history. | Persist status transitions and adversarial outcomes, enforce complete current-claim outcomes before `mapped`, and build the receipt from those records. |
| F7 | claim-unmet | C52 / P17 recorded edges | medium | A `jq` enumeration found only 16 xrefs. The receipt records B-01 dependencies on B-02 and B-04 at `rebuild-coverage-receipt.json:1622-1649`, but `mcp-server/src/index.ts:71-106,208-253` imports and registers tool arrays owned by B-05, B-06, B-07, and other subsystems without corresponding edges. This violates P17’s “every cross-subsystem data flow or dependency” acceptance check. | Record the missing registry dependencies with validated citations and extend the coverage denominator to include known cross-subsystem imports. |
| F8 | defect | Rebuilt B-03 claim / `rebuild-coverage-receipt.json:619` | medium | The published claim calls claim rows immutable, and `rebuild-depth-receipt.json:1065` says supersession occurs “rather than updating a row.” Both invalidation and supersession execute `UPDATE claims SET valid_until_sha` at `mcp-server/src/tools/claims.ts:389` and `:465`. | Supersede or correct the claim and change its adversarial result; describe the history as preserved through a controlled predecessor update plus successor insertion. |
| F9 | contract-drift | Project identity / `docs/index.md:1-3` | medium | The receipt identifies `github.com/nfeldman/amanuensis` at `rebuild-coverage-receipt.json:8`, but the promoted report’s primary identity is the worktree basename `amanuensis-reader-lenses`. `renderers.py:275-283` falls back to that basename when onboarding omits `**Codebase**:`, contrary to `spec.md:863-864`. | Record the canonical project name during onboarding or derive it from project binding metadata, never from the worktree directory. |
| F10 | defect | `dev/test-rebuild-coverage.mjs:255` | low | `file` classifies the new gate as binary data, and `rg -n pairKey` reports only a NUL byte near offset 10704. The source embeds a raw NUL as the pair separator, impairing normal source-search and review tooling. | Replace the raw byte with an escaped `\"\\0\"` source literal. |

## Notes

All other listed regression commands passed.
All sabotage was restored; the tracked tree had no residual review edits.