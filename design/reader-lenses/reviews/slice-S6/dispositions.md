# slice-S6 review dispositions

Run `20260912-095635-fix-slice-S6`. One reviewer (codex), so the `/name` suffix is dropped.
Every finding was reproduced or refuted against the tree before any repair. The reviewer's
`high` findings on the gates (F1, F2, F5) are fixed with red proofs; F6 is verified and
**not** fixed, and says why below rather than claiming a repair it did not get.

| F1 | verified: yes | action: fixed | reproduced: removed `await before.stop();` from rebuild-self-conspectus-store.mjs:248 and the P16 gate still exited 0 ("GATE P16 GREEN") — its arm reimplemented §12.1 in a scratch workspace and never invoked the driver. Driver now takes `--workspace`; gate arm 0 runs it against a throwaway workspace (test-rebuild-readback.mjs:302-460). Sabotage re-applied against the sharpened gate: exit 1, "the driver exited 1: the server process N is still addressable (alive); refusing to delete"; undone, exit 0. green 790cac4 |
| F2 | verified: yes | action: fixed | reproduced: P16 exited 0 with `repository_sha` = forty zeroes (syntax-only check at test-rebuild-readback.mjs:688); in a depth-1 clone matching test.yml:94 P17/P19/P18 exited 0, printing "revision ancestry not evaluable in a shallow clone". New dev/receipt-provenance.mjs resolves every cited revision incl. `repository_sha`, and unevaluable ancestry is RED, not note-and-pass; the mcp-server job now checks out `fetch-depth: 0`. Red both arms: zeroed sha → all four exit 1; depth-1 clone → P17/P19/P18 exit 1. green f71dfb8 |
| F3 | verified: yes | action: rejected | reproduced verbatim in a `git archive` + fresh `git init` snapshot: P17 exit 1 on "258ccd28fc199876e82655111d3e532b6757e389, 258ccd2 do not resolve", P19/P18 exit 1 on `0fee11b0…`. But all four resolve and are ancestors of HEAD in the deliverable (`git rev-parse` on each; 312 commits, non-shallow), so the receipts are bound to revisions included in the branch. The exit 1 is the gate correctly refusing provenance it cannot see — nothing distinguishes a discarded history from a fabricated sha. The remediation asked for ("verifiable without discarded Git history") directly contradicts F2's ("resolve every receipt provenance revision"), and satisfying it would weaken the binding F2 asks to strengthen. Supported checkout is full history, now enforced in CI by F2's fix |
| F4 | verified: yes | action: rejected | reproduced only in the same truncated snapshot: `RED fixture:revision:resolves: fatal: ambiguous argument 'b8b566f^{tree}'` then `RED baseline-report:derived`. In the worktree the listed regression command exits 0 — "historical A0 baseline complete: 297/297 obligations; 199 historically authoritative objects at b8b566f" — so C53's requirement that the checker stay green holds. The `derived` mismatch is downstream of the unresolvable fixture revision, not independent. The roadmap job that runs this checker already uses `fetch-depth: 0` (test.yml:14-16) |
| F5 | verified: yes | action: fixed | reproduced by reading rebuild-self-conspectus-store.mjs:246-252: only `before.pid` was probed, so any other server holding memory.db kept its handle across `rmSync`. Driver now enumerates every live holder of the storage dir and the three db files before deleting and refuses if any remain — and refuses when it cannot enumerate them at all. Red proof: a second server held open → exit 1, "process(es) 40538 still hold it open", memory.db survived; no squatter → exit 0. P16 carries that squatter arm as the positive control, so an empty holder list is not a green that has never seen a holder. green 4371b96 |
| F6 | verified: partly | action: not fixed | Part 2 **rejected**: `upsert_subsystem` is not a second door — subsystems.ts:109-118 calls `enforceMonotonicTransition` *and* `enforceForwardPrerequisites`, with a comment recording that slice-S3 F3/codex closed exactly this. Parts 1 and 3 **verified**: invariants.ts imposes no `mapped` prerequisite (the `default:` branch covers it), and record-rebuild-depth.mjs:388-403 `ladderFor()` synthesizes every rung from an `order` array with `tool`, `session_id` and `ref_sha` hardcoded. Worse than reported: test-rebuild-depth.mjs:364-390 asserts all of those synthesized fields, so that assertion cannot turn red — a second zero-denominator green. Not fixed because C43's "record each challenge outcome before `mapped`" has nowhere to be recorded: `claim_validity_events` admits only asserted/invalidated/superseded/revalidated, and no table holds a per-claim challenge outcome. Closing it needs a schema addition, a tool to write it, P19's adversarial pass re-run over the live store, and both receipts regenerated — past this session's 120-minute budget, and the tractable partial version ("≥1 adversarial record per subsystem") is the non-emptiness check C43's own evidence rejects. No later packet exists to defer to: every packet P1–P19 is `done` |
| F7 | verified: yes | action: not fixed | reproduced: 16 xrefs in rebuild-coverage-receipt.json, and index.ts:71-106,208-253 imports and registers tool arrays owned by B-05, B-06, B-07 and others with no corresponding edge, so P17's "every cross-subsystem data flow or dependency" is not met. Not fixed because the edges have to be written into the live store through `add_xref` with validated citation tokens and the coverage receipt regenerated from it; hand-writing them into the committed receipt would diverge it from its source and fake the citation validation. Needs the same regeneration pass as F6 |
| F8 | verified: yes | action: fixed | reproduced: the B-03 claim called the row "immutable" and the upheld challenge said supersession happens "rather than updating a row", while claims.ts:389 and :465 both execute `UPDATE claims SET valid_until_sha`. Corrected both texts to the controlled close-plus-insert the code performs, and added a two-sided gate (test-rebuild-coverage.mjs) that reads claims.ts: red while the code updates the interval and a receipt denies it, and red in the other direction if the code stops updating and the check starts measuring nothing. Red proof: contradiction restored → P17 exit 1 naming the claim; corrected → exit 0. The live store still holds the old wording; the next regeneration reconciles it |
| F9 | verified: yes | action: not fixed | reproduced: docs/index.md:1 is `# amanuensis-reader-lenses` (the worktree basename) while rebuild-coverage-receipt.json records `project_key: github.com/nfeldman/amanuensis`; renderers.py falls back to the basename when onboarding omits `**Codebase**:`. Not fixed because the promoted docs/ is byte-matched against its projection contract by P18's gate, so changing the rendered identity means re-rendering, re-verifying and re-promoting all 48 pages — I would not have had time to verify that publication end-to-end, and a half-promoted docs/ tree is worse than the wrong title |
| F10 | verified: yes | action: fixed | reproduced: `file` reported "binary data" and `rg -n pairKey` returned only "binary file matches (found \"\\0\" byte around offset 10704)"; two raw NULs at offsets 10704 and 20493. Replaced with `\0` source escapes — `file` now reports UTF-8 text and `rg -n pairKey` prints its five lines. green 4371b96 |

## What ran

All 15 regression commands of P16/P17/P18 (and P19's five) exit 0, including
`node dev/check-living-conspectus.mjs` and `node dev/test-living-conspectus.mjs`.
All four S6 gates exit 0 on the full worktree after every fix.

Every sabotage in this session was applied and undone; `git status` is clean apart from the
committed fixes, and no tracked receipt carries a review edit.

## Left standing

F6, F7 and F9 are verified and unrepaired, and they share one cause: the rebuilt
self-conspectus lives in an untracked store, and three of these repairs are writes to that
store plus a receipt regeneration. F6 is the one that matters most — not for the missing
`mapped` prerequisite alone, but because the depth gate's ladder assertion asserts over a
reconstruction of itself and therefore cannot fail.
