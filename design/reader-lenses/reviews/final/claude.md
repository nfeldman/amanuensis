[claude-code:unrecognized_model] {"model":"claude-fable-5-1[1m]","query_source":"sdk"}
## Findings

| Id | Kind | Location | Severity | Evidence | Proposed fix |
|---|---|---|---|---|---|
| F1 | defect | mcp-server/test-finding-partition.mjs:840 | high | `node mcp-server/test-finding-partition.mjs` exits 1 on a clean tree: "B01-1 does not link to ../resolved-findings.md; B01-3 does not link to ../resolved-findings.md" and "no subsystem page links to ../resolved-findings.md#b01-1". The gate (lines 825-846) requires the subsystem page to emit per-finding anchored links `resolved-findings.md#<id>` for resolved findings, but spec §6.2 (last bullet) and §7.3 item 6 specify a **collapsed count** with a page-level link, which `render_subsystem` correctly emits at renderers.py:1226-1232. The renderer is spec-compliant; the gate over-asserts and is red in CI (test.yml:216). | Align the gate with §6.2/§7.3: assert the collapsed-count link to `resolved-findings.md` (no per-finding anchor). To still exercise `_build_xref_index` routing per the P2 acceptance, seed a `[[B01-1]]` reference in the fixture's subsystem prose and assert it resolves to `resolved-findings.md#b01-1`. Do not change the renderer. |
| F2 | gate-cannot-red | mcp-server/test-vocabulary-source.mjs:546 | medium | Spec §13 lists "a tool validator accepts a value the source does not carry" as a red condition for this gate, but the "tool validators are the generated arrays" check (538-554) is purely textual — it only greps for the absence of a literal, the import identifier, and the import path. Patching evidence.ts:47 to `requireEnum(args,"kind",[...EVIDENCE_KINDS,"bogus"])` keeps `test-vocabulary-source.mjs`, `check-evidence-vocabulary.mjs`, and `gen-vocabulary.mjs --check` all exit 0 while `add_evidence` then accepts `kind:"bogus"` (verified). | Add a behavioural arm that drives each bound validator (e.g. `add_evidence`, `set_disposition`, `add_claim`) with a value absent from the vocabulary source and asserts it is refused, so a validator that accepts an out-of-source value turns the gate red. |
| F3 | gate-cannot-red | materializer/test-overview-truthfulness.py:814 | low | `no_composite_score` (813-819) matches only `(health\|overall\|composite\|readiness) (score\|index\|grade\|rating)`, bare `score`, `<progress`/`<meter`, and block glyphs. A composite health index rendered as `Health: 72%` (label + percentage, no "score/index/grade/rating" word) evades it, though C32/BP26 forbid any health index in the overview; `Health score: 72%` is caught, `health: 72%` is not (verified against the regex and by sabotage). | Add a check that no line in the overview's status region pairs a label with a bare `\d{1,3}\s?%` or an `n of m`/`n/m` composite, so a percentage/ratio "health index" turns the publish red. |

## Notes

- 15 packet gates are green; P2 is red on the clean tree (F1). P16/P17/P18/P19 and the regression `check-living-conspectus.mjs` are red **only** because receipt/fixture revisions (331078…, 0fee11…, 258ccd…, b208b6…, b8b566f) were squashed out of this history-free snapshot; every one of their non-SHA assertions passes, and CI checks out `fetch-depth: 0` and resolves them — snapshot artifacts, not code defects.
- All other regression commands (32, deduplicated) pass in an isolated copy.
- Sabotage sweep (~90 mutations across every passing gate) turned each gate red on its assigned behaviour, many via substrate guards (census reconciliation, the attention-label vocabulary guard, the read-back state axis, the materializer view-probe refusal). describe_locus/get_attention/get_history budgets, owners-never-truncated, the omission ledger, unknown[]'s five pair-unit sources, kind inference, the two reachability reasons, and the search-index/accessibility arm (driven in Chromium, no-JS path) are all red-capable.
- Sabotage 1(b) (finding_state_current legacy `fixed→open`) is caught by P2 via its legacy row B01-4 — not a gap.
- Two low-value gaps not filed: P8's `LEGACY_PAGE_PATHS` omits the 7 new pages so its "no page retired" check can't fire for them (but P9's publish checks + the coverage axis cover them); the no-git `examined-stale` authorization downgrade isn't asserted in test-locus-standing.mjs (P6) but P5 catches it.
