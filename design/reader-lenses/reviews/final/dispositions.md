# Final review — dispositions

Run `20260912-120314-fix-final`, branch `reader-lenses`. Every finding from
`codex.md` and `claude.md` was reproduced or refuted against the code before any
repair. Sabotages applied to measure a gate's red-capability were applied and
undone locally; the tracked tree carries none of them.

| F1/codex | verified: yes | action: fixed | reproduced: appended an exported-but-unregistered `orphan_probe` ToolDefinition to evidence.ts; `gen-tool-inventory.mjs --check` exited 0 while check-tool-schemas.mjs compiled 202 schemas against 201 advertised tools. --check now reconciles every name declared in src/tools/*.ts against tools/list and fails on a source-only or doubly-declared name (scripts/gen-tool-inventory.mjs:33-95); the name scan is narrowed to objects publishing description+inputSchema so response sections are not counted as tools. Under the same sabotage --check and GATE P15 both turn red; restored. b924e4d |
| F2/codex | verified: yes | action: deferred (P21) | reproduced both arms: `markStale.run("git-driftt", …)` at git.ts:272 left GATE P1 GREEN and check-evidence-vocabulary.mjs exit 0; the SKILL comparison at check-evidence-vocabulary.mjs:158-194 covers 2 of 25 vocabularies, so SKILL.md's subsystem-status ladder and table are compared against nothing. Not repaired inside the 120-minute budget — see F4/codex, which carries the same root cause (literal writers, unconsumed generated enums). Medium; no later packet exists, so the lane owner must schedule it. |
| F3/codex | verified: yes | action: fixed | reproduced: P6's fixture held no verified-fixed event at all, so a branch at locus.ts:724 skipping every verified-fixed event left GATE P6 GREEN. The fixture now carries a fourth commit and B01-6, repaired at `head` and verified at `verified`, read at four cuts through both `defects` and `history_pointer` (test-locus-standing.mjs:284-289, 417-446, 1116-1180). Under the reviewer's sabotage P6 now turns red at the verification cut; applied and undone. red 0f8c41a, green dcd02b2 |
| F4/codex | verified: yes | action: deferred (P21) | reproduced: `rg STALE_REASONS src/` returns only vocabulary.ts's own declaration and type alias — no consumer; git.ts:272-275 and standing.ts:416-424 write bare literals. Not repaired inside the budget. The repair is to type each writer's argument from the generated enum so the compiler rejects an out-of-source value, and to make the SKILL comparison table-driven over every vocabulary SKILL.md publishes (F2/codex). Medium; no later packet exists. |
| F5/codex | verified: yes | action: fixed | reproduced: verify_finding_fix stored `resolveCommit(ctx, current.fix_sha)` — the repair SHA — as the verification event's only revision (findings.ts:348-353), and add_finding's opening event stored none (findings.ts:164-168); the replay cut by `fix_sha` (locus.ts:723). P6's new four-cut assertion failed on the current tree with `at the repair, before verification: "verified-fixed"`. finding_resolution_events.effective_sha added with an additive migration backfilled from fix_sha (schema.sql:852-860, db.ts:125-135); every writer populates it — creation from the finding's ref_sha, repair from the fix commit, overturn from its disproving evidence's revision, verification from the verification evidence's revision — and both replay sites cut by it. red 0f8c41a, green dcd02b2 |
| F6/codex | verified: yes | action: deferred (P21) | reproduced by reading: evidence.ts:46, findings.ts:123 and dispositions.ts:61 take `ref_sha` through `requireString` and store it unresolved, while locus.ts:805 reports `revision_bound: entry.row.ref_sha !== null` — true for any non-empty string. **This is a high left unrepaired at the 120-minute budget.** The repair (hoist findings.ts:45's `resolveCommit` into a shared helper, resolve at each of the three writers, and require an embedded citation's revision to agree with the record's) has a wide blast radius across the fixture suite and could not be completed and regressed inside the remaining time. No later packet exists; the lane owner must schedule it before this work is called done. |
| F7/codex | verified: yes | action: fixed | reproduced by runtime probe: `resolveLocus(db, "nosymbol", "symbol")` returned `split_at:-1`, path `"nosymbo"`, symbol `"nosymbol"`. `asSymbol` now refuses a value whose shape does not carry a non-empty path, a colon, and a non-empty symbol (standing.ts:284-297); the inference ladder proves that shape before reaching the constructor, a caller-supplied kind does not. red 424a173, green 117c738 |
| F8/codex | verified: yes | action: fixed | reproduced by runtime probe: `requireWorkspaceCitation("src/pkg@v1/file.ts:sym@abcdef0")` is accepted (first colon, last @) while the read surfaces reduced it to `src/pkg`. P6 now seeds that citation and read `src/pkg` carrying B01-5 on the current tree. helpers.parseCitation is the one parser, used at locus.ts:652, 2267, 2342, 3384 and impact.ts:178. red ec31460, green d10a361 |
| F9/codex | verified: yes | action: deferred (P21) | reproduced by reading: render_master_plan derives its open count from `findings.status` (renderers.py:904) while render_subsystem reads `finding_state_current`; get_dashboard and list_subsystems carry the same duplicate predicate. Not repaired inside the budget. Medium; no later packet exists. |
| F10/codex | verified: yes | action: fixed | reproduced: `node mcp-server/test-finding-partition.mjs` exited GATE P2 RED on a clean tree — the gate demanded per-finding anchored resolved links from subsystem pages while spec §6.2's last bullet and §7.3 item 6 require a collapsed count, which renderers.py:1226-1232 emits. The gate now asserts the collapsed link and reads _build_xref_index routing on a surface that references the finding: `[[B01-1]]`/`[[B01-2]]` seeded into the subsystem's jump_in_reading. Routing every id to findings.md in _build_xref_index turns P2 red on cross-link-anchor; applied and undone. 9c4b10b |
| F11/codex | verified: no | action: rejected | not reproducible in a checkout with history: all five commands pass here — `node dev/check-living-conspectus.mjs` (297/297 obligations at b8b566f), `dev/test-reader-lenses-dogfood.mjs` GATE P18 GREEN, `dev/test-rebuild-coverage.mjs` GATE P17 GREEN, `dev/test-rebuild-depth.mjs` GATE P19 GREEN, `dev/test-rebuild-readback.mjs` GATE P16 GREEN. `git cat-file -t 331078` resolves here and the reviewer's snapshot HEAD `0abc1932` does not, so the receipt revisions were squashed out of the review snapshot. A snapshot artifact, not a code defect; CI checks out at `fetch-depth: 0`. |
| F1/claude | verified: yes | action: fixed | same defect as F10/codex; see that row. 9c4b10b |
| F2/claude | verified: yes | action: deferred (P21) | reproduced: patched evidence.ts:47 to `requireEnum(args, "kind", [...EVIDENCE_KINDS, "bogus"])`; `test-vocabulary-source.mjs`, `check-evidence-vocabulary.mjs` and `gen-vocabulary.mjs --check` all exited 0, because the "tool validators are the generated arrays" check (538-554) only greps for an absent literal and a present import. Restored. Not repaired inside the budget: the fix is a behavioural arm that drives each bound validator with an out-of-source value against a live store and asserts refusal. Medium; no later packet exists. |
| F3/claude | verified: yes | action: deferred (P21) | reproduced by reading test-overview-truthfulness.py:813-819: the regex requires a "score/index/grade/rating" word, so `Health: 72%` evades it while `Health score: 72%` is caught. Not repaired inside the budget. Low; no later packet exists. |

## What was run

- Gates: P1, P2, P5, P6, P7, P14, P15, P16, P17, P18, P19 — all green on the tracked tree.
- Regression: `biome check src/`, `npm run build`, `check-sql-identifiers.mjs`,
  `check-tool-schemas.mjs`, `gen-tool-inventory.mjs --check`, `check-evidence-vocabulary.mjs`,
  `test-smoke.mjs`, `test-invariants.mjs`, `test-resolution-proof.mjs`, `test-change-impact.mjs`,
  `test-projection-custody.mjs`, `test-derived-staleness.mjs`, `test-materializer.py`,
  `test-readback.py`, `dev/check-living-conspectus.mjs`.
- No test or gate was weakened. The P2 realignment replaced an assertion the binding spec
  contradicts with two that the spec and the P2 acceptance each require, and left the
  coverage-axis red arm intact and firing.

## Carried debt

Four mediums (F2, F4, F9/codex; F2/claude), one low (F3/claude), and **one high (F6/codex)**
were verified but not repaired inside the 120-minute budget. The plan holds no later packet to
carry them, so none of them is discharged by this run.
