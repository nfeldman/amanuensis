## Findings

| Id | Kind | Location | Severity | Evidence | Proposed fix |
|---|---|---|---|---|---|
| F1 | gate-cannot-red | mcp-server/test-consumer-route.mjs / mcp-server/scripts/gen-tool-inventory.mjs:34 | high | Added a compiling exported `ToolDefinition` without registering it; P15 and `gen-tool-inventory.mjs --check` still exited 0, while the schema checker found 202 tool schemas against 201 advertised tools. | Compare every tool name discovered in source modules with `tools/list`; fail on source-only or multiply defined tools. |
| F2 | gate-cannot-red | mcp-server/test-vocabulary-source.mjs | medium | Changed the production `git-drift` writer to `git-driftt`; P1 still printed `GATE P1 GREEN`. Changing SKILL.md's `concerns` status to `concerns-broken` also left P1 and the four-way checker green. | Exercise every production enum consumer and independently compare every enum list published by SKILL.md. |
| F3 | gate-cannot-red | mcp-server/test-locus-standing.mjs | high | Inserted a branch at `src/tools/locus.ts:724` that skipped every `verified-fixed` event; P6 still printed `GATE P6 GREEN`. | Seed a repair and a later verification revision, then assert defect and history results at cuts before repair, between repair and verification, and after verification. |
| F4 | claim-unmet | C48 / mcp-server/src/vocabulary.ts:39 | medium | `rg` found no consumer of generated `STALE_REASONS`; `tools/git.ts:272-275` and `standing.ts:416-424` write literals. The SKILL comparison at `check-evidence-vocabulary.mjs:158-194` checks only two of 25 vocabularies. | Type writer outputs from generated enums and expand the independent SKILL comparison to every vocabulary it publishes. |
| F5 | claim-unmet | C16 / mcp-server/src/tools/findings.ts:164 / mcp-server/src/tools/locus.ts:724 | high | Initial events have no revision, while verification events store the repair SHA rather than the verification evidence SHA. A temporary-repository probe verified at descendant B, but `describe_locus(as_of_sha=A)` already returned `verified-fixed` at repair A. | Add an event `effective_sha`; populate it from finding creation, repair, overturn evidence, or verification evidence as appropriate, and replay applicable events by that revision. |
| F6 | defect | mcp-server/src/tools/evidence.ts:46 / mcp-server/src/tools/findings.ts:123 / mcp-server/src/tools/dispositions.ts:61 | high | These writers accept `ref_sha` with `requireString` and store it without resolving it; `locus.ts:805` nevertheless reports every non-null value as `revision_bound: true`. | Resolve every revision in the bound workspace before writing and require embedded citation revisions to agree with the record revision. |
| F7 | defect | mcp-server/src/standing.ts:285 | medium | Runtime probe `resolveLocus(db, "nosymbol", "symbol")` returned `split_at:-1`, path `nosymbo`, and symbol `nosymbol`; the input schema allows this value. | Reject forced symbol loci without a non-empty path, colon delimiter, and non-empty symbol before slicing. |
| F8 | defect | mcp-server/src/tools/locus.ts:652 | medium | Citation ingress deliberately uses the last `@`, permitting a valid path such as `src/pkg@v1/file.ts:sym@abcdef0`; locus readers use `split("@")[0]`, reducing that path to `src/pkg`. The same parser occurs at lines 2267, 2342, and 3384. | Use one shared citation parser based on the last revision delimiter and first symbol delimiter on every read surface. |
| F9 | claim-unmet | C27 / materializer/amanuensis_materializer/renderers.py:904 | medium | `render_master_plan` derives its displayed open count from `findings.status`, while subsystem rendering reads `finding_state_current`; `get_dashboard` and `list_subsystems` retain the same duplicate predicate. | Join `finding_state_current` for every resolution-state count and remove the remaining status-derived resolution predicates. |
| F10 | contract-drift | P2 / design/reader-lenses/spec.md:899 / materializer/amanuensis_materializer/renderers.py:1226 | medium | The P2 gate exits 1 because it demands individual resolved-finding links from subsystem pages, while spec §6.2 and §7.3 require only a collapsed resolved count linking to `resolved-findings.md`; the renderer follows the spec. | Reconcile P2 acceptance with the binding spec; assert the aggregate subsystem link and test individual xref routing on a surface that actually references the finding. |
| F11 | regression-fail | dev/receipt-provenance.mjs:24 | medium | Five of 32 unique regression commands failed: `check-living-conspectus`, `test-reader-lenses-dogfood`, `test-rebuild-coverage`, `test-rebuild-depth`, and `test-rebuild-readback`. Each cites receipt revisions absent from snapshot HEAD `0abc1932`. | Supply the receipt's Git objects/full history to the review checkout, or use independently verifiable bundled provenance that survives a history-free snapshot. |

## Notes

- Baseline result: 14 of 20 packet gates passed.
- P10's static and read-back arms passed, but Chromium exited before exposing a debugging endpoint, leaving the browser accessibility arm unverified.
- All 32 unique regression commands ran: 27 passed and five failed as listed in F11.
- Every sabotage was restored; the final tracked diff is empty and only the supplied `REVIEW/` directory remains untracked.