## Findings

| Id | Kind | Location | Severity | Evidence | Proposed fix |
|---|---|---|---|---|---|
| F1 | contract-drift | mcp-server/src/tools/carried.ts:412 | medium | `attach_carried_evidence` takes `role` as free text (`optString(args, "role") ?? "supports"`), while both siblings validate it — ran a probe against `dist`: `attach_carried_evidence(role:"not-a-role-at-all")` returned `{"ok":true,…,"role":"not-a-role-at-all","action":"attached"}` and stored that row, whereas the same value gave `role must be one of: symptom, root-cause, fix-anchor, fix-verification, compensating` from `attach_evidence_to_finding` and `role must be one of: supports, contradicts, linchpin, compensating` from `attach_evidence_to_disposition` (evidence.ts:87, :122). spec §5.2a calls this tool "the `finding_evidence` shape"; `carried_finding_evidence.role` (schema.sql:656) carries no `CHECK`, so the tool is the only enforcement point, and the table's triggers refused both `UPDATE … SET role='supports'` \("carried finding evidence attachment is immutable"\) and `DELETE` \("cannot be deleted"\), so the bad value is permanent | validate with `requireEnum(args, "role", …)` against the disposition/finding role vocabulary, and declare the enum in `contracts/conspectus-vocabulary.json` with a `CHECK` on `carried_finding_evidence.role` so `migrateVocabularyChecks` carries it, as §5.2 requires of the other two carried enums |
| F2 | defect | mcp-server/src/tools/carried.ts:311 and :339 (with begin_carry_run, :137 / :142) | medium | `archived_anchor` is optional on `begin_carry_run` for every `source_kind`, and `carry_finding` writes `run.archived_anchor ?? ""` into `carried_findings.archived_anchor_sha` and into the pre-recorded `archived-terminal` outcome's `ref_sha`. Probe against `dist`: `begin_carry_run(source_kind:"store", archived_store_id:"store-probe…", …)` with no `archived_anchor`, then two `carry_finding` calls, produced `archived_anchor_sha:""` on both records and `ref_sha:""` on the `archived-terminal` outcome, and `finish_carry_run` returned `ok:true`. Both columns are `NOT NULL` precisely so the archive's revision is recorded (spec §5.2, §5.3); the empty string satisfies the constraint while recording nothing, the rows are append-only, and `GATE CF1` stays green. Every other revision field in this diff is resolved at ingress or refused | require `archived_anchor` in `begin_carry_run` for `source_kind` in `('store','export')`, with the same shape as the existing `archived_store_id` refusal at :175, and drop the `?? ""` fallbacks so a missing anchor cannot be written |

## Notes

- All 53 deduped regression commands were run. 50 pass. The 3 that fail (`dev/test-rebuild-readback.mjs`, `dev/test-rebuild-coverage.mjs`, `dev/test-reader-lenses-dogfood.mjs`) fail only on revision ancestry — e.g. "revision(s) 7c1c1a9f…, dee59d3e… are not ancestors of HEAD".
- Gates: P0–P7 and P9 green. P8 exits 2 (`CANNOT RUN: no live store at .amanuensis/memory.db`), P10 and P11 exit 1. Every failing assertion in P10/P11 names an unreachable revision (`61bc6b5c…`, `d206ad7`, `c073404`, `1d6516a`) or the 15 `design/survey-depth/` files the snapshot dropped plus one tracked `mcp-server/node_modules` path.
- Those are review-snapshot artifacts, not code defects: the snapshot is one squashed commit (`d2445f4`), `git rev-parse --is-shallow-repository` is `false`, so `historyIsComplete` (dev/receipt-provenance.mjs:40) correctly reports a complete history and every cited revision then legitimately fails ancestry. The `cannot run` escape is specified for shallow clones only (spec §7.1) and behaves as specified. I could not exercise P10/P11 red-capability here for the same reason.
- I ran 20 sabotages across P0–P9; every one turned its gate red. Attacked: quality-ladder inversion, dropped `evidence_ids`, over-strict §2.3 advance, dropped `ledger_digest` condition, unresolved `detected_sha`, removed `materialize_docs` refusal, `anchored = true`, removed tree-path check, removed decline/anchored-term conflict, always-true ancestry, `record_carried_outcome` writing `archived-terminal`, dropped `ruled-out` session-evidence rule, `WIRE_BUDGET` raised to 1e9, schema rewriting a domain row, `CREATE TABLE` without `IF NOT EXISTS`, ledger-derived projection denominator, emptied carried census, parity check ignoring the reference side, dropped and falsified `pecia-dogfood.md` rows, D0's B2 made unfailable, D0 trusting a receipt `verdict`. Each file was restored with `git checkout --`; the tree is clean and the 12 gates re-run byte-identically.
- P10's declared control-gate red proof holds: silencing D0's B2 and making D0 read `"verdict": "green"` instead of recomputing B3 both turned P5/`GATE D1` red.
- C33 survives inspection. `dev/test-rebuild-depth.mjs` loses 14 `check(` calls but re-registers all 14 as `checkScoped`, gated on `describesSameStore`; nine report `cannot run` with exit 2, never green, RED wins over cannot-run, two controls (a `fb9f1c4` replay and a relabelled-receipt forgery) run in both states, and `.github/workflows/test.yml:390-406` re-derives the one-store/two-stores decision independently of the gate. Each of the nine iterates `expectedMapped()`, so their denominator really is P17's. `test-smoke.mjs`'s two replaced assertions are strengthened, not dropped.
- C43 verified mechanically: the gate command of every transitive dependency appears in each packet's regression list, 0 violations over all 12; `plan.json` and `REVIEW/packets.json` regression lists are identical.
- Contract checks pass: `gen-tool-inventory --check`, `check-tool-schemas`, `check-sql-identifiers`, `check-evidence-vocabulary`, `gen-vocabulary --check`/`--check-sql`, plus the two new ones (`check-concern-checklist`, `check-refusal-parity`), both wired into CI at test.yml:296 and :306. `append-only-tables.txt` names all eight new tables and P0's T0 assertion holds it against `schema.sql`. `EVIDENCE_KINDS` and `EVIDENCE_QUALITIES` are identical lists, so the §2.2 rank comparison across them is sound.
- No accessibility regression: `html_projection.py`'s only change is deriving the marker regex from `MARKER_KINDS`; the template is untouched, and the new *Carried obligations* and declination sections render as headings, lists and links with `aria-label`ed anchors and `abbr` labels, no new script, no interactive widget. Overview keeps `Carried defects undecided` as its own row beside `Findings open` (docs/index.md:33-34), per §5.6.
