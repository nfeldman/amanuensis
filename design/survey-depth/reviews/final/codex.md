## Findings

| Id | Kind | Location | Severity | Evidence | Proposed fix |
|---|---|---|---|---|---|
| F1 | regression-fail | dev/test-carry-receipt.mjs:677 | high | `node dev/test-carry-receipt.mjs` exited 2 because `.amanuensis/memory.db` is absent, so packet P8's gate never reached green despite the archive and committed receipt being readable. | Make the gate self-contained by reconstructing a temporary carry store from the archive, or package the store fixture needed to validate the receipt. |
| F2 | regression-fail | dev/receipt-provenance.mjs:24 | high | `test-survey-depth`, `test-survey-depth-acceptance`, `test-reader-lenses-dogfood`, `test-rebuild-coverage`, and `test-rebuild-readback` all exited 1. The snapshot has one commit, while receipts cite `61bc6b5`, `d206ad7`, `7c1c1a9`, and `dee59d3`; none is an ancestor of HEAD. P11 failed 14 of 38 assertions. | Preserve the referenced history in distributed snapshots, or replace ancestry-only witnesses with self-contained tree and object digests and re-record the receipts. |
| F3 | regression-fail | materializer/test-search-index.py:625 | medium | `cd materializer && python3 test-search-index.py` exited 1 after every static assertion passed because the selected Chromium process exited `-6` without reporting a debugging endpoint, leaving keyboard, live-region, and no-JS behavior untested. | Probe browser candidates for a working DevTools endpoint and fall through when one cannot launch; provision a runnable browser in the review environment. |
| F4 | regression-fail | dev/test-amanuensis-pecia-defects.mjs:79 | medium | `node dev/test-amanuensis-pecia-defects.mjs` exited 1: `pecia --help` failed on an unwritable uv cache, but only `spawnSync.error` was checked; the unchecked `pecia init` failure then caused an `ENOENT` at line 144. | Check subprocess exit statuses, report dependency failure explicitly, and run Pecia with a writable `UV_CACHE_DIR`. |
| F5 | defect | mcp-server/src/tools/git.ts:378 | medium | In a temporary repository, deleting a path owned by two ledger rows made `detect_changes` throw `UNIQUE constraint failed: scope_gaps.file_path, scope_gaps.kind`; no reconciliation was written. The read-only AxiomDB store contains 53 duplicate-owned paths. | Deduplicate absent paths before inserting `scope_gaps`, while separately preserving and reporting all affected owners. |
| F6 | claim-unmet | C18 / materializer/amanuensis_materializer/renderers.py:2280 | medium | `_vocabulary_state([], [{"ref_sha": "000…"}], nonexistent_workspace)` returned `declined`. Spec §4.4–4.5 says an unreachable declination is history and satisfies nothing, so it must render as `not recorded`. | Resolve declination revisions in the projection, select the newest reachable row as effective, and render unreachable rows only as history. |
| F7 | claim-unmet | C22 / mcp-server/src/schema.sql:622 | medium | Spec §5.4 says a mistaken outcome is corrected by a new visible record from the same archive, but the unique key on archived store and finding id plus carried.ts:286-296 rejects that second record. | Add append-only versioning or `supersedes_carried_id`, and adjust uniqueness and readers so a correction remains visible without overwriting history. |
| F8 | compactness | mcp-server/src/tools/carried.ts:686 | medium | A carried row with a 9,000-character `subsystem_id` produced an 18,474-byte `list_carried_findings` envelope against the 8,192-byte budget. The fallback cleared `symptom` but left other unbounded compact fields intact. | Bound all repeated identifiers at write time and truncate every unbounded compact field in the legacy-row fallback. |
| F9 | contract-drift | mcp-server/src/tools/carried.ts:594 | low | The list filter advertises `outcome` as an unrestricted string; an in-process call with `outcome: "typo"` returned an empty successful page. The single-source carried-outcome enum is used by the writer but not this reader schema. | Derive a list-outcome enum from `CARRIED_FINDING_OUTCOMES` plus `undecided`, advertise it in the schema, and validate it in the handler. |

## Notes

- Every initially green packet gate, P0–P7 and P9, turned red under its targeted sabotage; no `gate-cannot-red` was found.
- P8 was initially `CANNOT RUN`; its corrupted receipt did turn the gate red.
- P10 and P11 were already red before sabotage, so their selectivity could not be established.
- Of 53 unique regression commands, 45 passed and the eight failures are all reported above.
- All tracked-file sabotages were restored; only the supplied untracked `REVIEW/` directory remains.