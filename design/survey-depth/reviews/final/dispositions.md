# Dispositions for final

Eleven findings, two reviewers. Every finding was re-run or re-read at HEAD in the
worktree before it was dispositioned. Three did not reproduce there: they are the
review snapshot's environment, not the tree's behaviour, and reviewer claude reached
that reading independently for the same failures. Eight held; seven are repaired with
a red proof, one is deferred.

| F1/codex | verified: no | action: rejected | reproduced: no — `node dev/test-carry-receipt.mjs` at HEAD exits 0 and prints `GATE P8 GREEN — GATE CR1 GREEN`, reading the archive, the committed receipt and 22 live carried rows. The snapshot has no `.amanuensis/memory.db`, and exit 2 `CANNOT RUN` there is §8.9a's declared third state, not a false green: the gate never reports GREEN without a store. Making it self-contained would replace the archive it exists to read with a fixture of this lane's own assumptions |
| F2/codex | verified: no | action: rejected | reproduced: no — at HEAD `dev/test-survey-depth.mjs` (P10/D0) exits 0 and the five cited commands pass; P11's only failure names the two review documents, not ancestry. The cited revisions are this lane's own commits, and `snapshot_history` (run.sh:180-193) fetches a fixed list and by design refuses anything that is not an ancestor of `main`, so a receipt recorded *by* the lane can never be witnessed *in* a snapshot. That is a defect in the reviewer's harness, not in the tree; reviewer claude read the same failures the same way. Recorded, not repaired here: this session may not edit run.sh while the loop that invoked it is running |
| F3/codex | verified: no | action: rejected | reproduced: no — `cd materializer && python3 test-search-index.py` at HEAD exits 0 and prints `GATE P10 GREEN`, with the keyboard, live-region and no-JS assertions all run against a Chromium that launched. The reviewer's chosen binary aborted with `-6` in the snapshot; falling through to another candidate is a worthwhile hardening of the harness but the tree's behaviour under a browser that cannot launch — fail loudly, never skip — is what §8.0 asks for |
| F4/codex | verified: yes | action: fixed | reproduced: the gate exits 0 here (a writable uv cache), so the hole was confirmed by reading: `run()` checked only `spawnSync.error` and returned a status-1 `pecia init` happily, after which the missing `.pecia/config.yaml` surfaced as an unrelated ENOENT. A blanket status check in `run()` is wrong — several assertions are exactly "the resolver exited 1" — so the check is opt-in via `mustRun` for setup steps, and the `pecia --help` probe now reports `cannot run` on a non-zero exit as well as on a spawn error. dev/test-amanuensis-pecia-defects.mjs:52-74; 8/8 still pass; 1f36a1c |
| F5/codex | verified: yes | action: fixed | reproduced: A9b in mcp-server/test-scope-reconciliation.mjs deleted a path two subsystems own and `detect_changes` threw `UNIQUE constraint failed: scope_gaps.file_path, scope_gaps.kind`, aborting the transaction that rewrites the gaps, so no reconciliation was written at all. Fixed by writing one gap row per absent path; every owner is still flagged in `file_ledger` by the `mark(absent, …)` above, so no owner is lost. red 9c6f4a3, green ba23e02 |
| F6/codex | verified: yes | action: fixed | reproduced: a new C18 assertion in materializer/test-unmeasured-coverage.py showed `_vocabulary_state([], [{ref_sha: "0"*40}], workspace)` returning `declined`, against spec §4.4's "rows anchored to a revision the repository no longer has render as history and satisfy nothing". Fixed with `_declination_resolves`; the term arm's `anchor_opens` is the wrong predicate here because a declination carries a bare revision, not a `path#symbol@sha` citation. Both directions are asserted — unreachable renders `not-recorded`, reachable still renders `declined`. red 9c6f4a3, green ba23e02 |
| F7/codex | verified: yes | action: deferred to P12 | reproduced: the contradiction is real and is in the prose, not in C22. `UNIQUE (archived_store_id, archived_finding_id)` (schema.sql:622) and carried.ts:288-297 both refuse a second record, while spec §5.4's closing line and the schema comment above the outcomes table say "a mistaken outcome is corrected by a new carried record from the same archive". C22 itself claims only that a carried finding reaches exactly one terminal outcome with the four authority rules, and that is met and gated. So a mistaken outcome is at present not correctable at all, which is a design gap and not a line to edit: `supersedes_carried_id` plus append-only versioning changes the obligation count, the census, §5.7's resolver and GATE CF1 together. Deferred rather than half-landed inside this session's budget; the prose must not be realigned to describe a mechanism nobody has built |
| F8/codex | verified: yes | action: fixed | reproduced: CD5 in mcp-server/test-carried-findings.mjs — one legacy row with a 9,000-character `subsystem_id` answered 9,197 bytes against the 8,192-byte budget, the single-row fallback having cut only `symptom` and `archived_finding_id`. Fixed on both sides: `carry_finding` bounds `subsystem_id` and `begin_carry_run` bounds `archived_store_id` at MAX_ID_LENGTH, and the fallback truncates all three repeated identifiers. red f6beacc, green 19d1e63 |
| F9/codex | verified: yes | action: fixed | reproduced: CD4 — `list_carried_findings({outcome: "typo"})` returned a successful empty page, so a typo read as "none carry it". Fixed by deriving `LIST_OUTCOME_FILTERS` from `CARRIED_FINDING_OUTCOMES` plus `undecided`, advertising it in the schema and refusing anything else. red f6beacc, green 19d1e63 |
| F1/claude | verified: yes | action: fixed | reproduced: CD1 — `attach_carried_evidence(role: "not-a-role-at-all")` was accepted and the row stored, while both siblings refuse an unknown role. Fixed with `requireEnum` against the disposition-evidence vocabulary, advertised in the input schema. No `CHECK` was added to `carried_finding_evidence.role`: the column is append-only in both directions and the tool is its only writer, so the enforcement point is the tool. red f6beacc, green 19d1e63 |
| F2/claude | verified: yes | action: fixed | reproduced: CD2 — `begin_carry_run(source_kind: "store")` with no `archived_anchor` was accepted, and `carry_finding` wrote `""` into `carried_findings.archived_anchor_sha` and the archived-terminal outcome's `ref_sha`, both NOT NULL and both append-only. Fixed by requiring `archived_anchor` for store and export in the same shape as the `archived_store_id` refusal beside it, and replacing the two `?? ""` fallbacks with `requireCarriedAnchor`. CD3 holds the store to no empty anchors. red f6beacc, green 19d1e63 |

## The gate the launcher named

P11 (`GATE A1`) was RED at HEAD when this session opened, on one assertion of 28: the
receipt's ledger accounted for 649 paths while 651 were tracked, the two unledgered
paths being `reviews/final/claude.md` and `reviews/final/codex.md`. Reproduced first,
before anything was changed. It is repaired by re-recording the receipt over this
tree — never by relaxing the inventory, which is what the gate's own message says —
and the re-record is the last act of the session, because this dispositions file is
itself a tracked path the receipt has to account for.
