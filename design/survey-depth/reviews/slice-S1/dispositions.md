# Fix dispositions — slice S1

Run `20260914-185211-fix-slice-S1`, against `design/survey-depth/reviews/slice-S1/codex.md`.
One row per finding. Every sabotage named below was applied and undone locally; the tree
carries none of them.

| F1 | verified: yes | action: fixed | reproduced: deleted both scope_reconciliations triggers at schema.sql:229-234 and the gate exited 0, reporting `append-only behaviour proved on 46 of 81 declared table(s)` and `GATE P0 GREEN` — appendOnlyTables() ended in `.filter((p) => p.immutable && p.undeletable)`, so the sabotaged table left the denominator with the numerator. The denominator is now the checked-in mcp-server/contracts/append-only-tables.txt (83 rows), T0 lays it against schema.sql in both directions, and T1/T2 iterate the required list. The same sabotage now exits 1 with T0, T1 and T2 red, each naming both triggers. T2 also seeds through openDatabase, so the lane's own seven tables are probed rather than reported as `near ")": syntax error`; 53 of 83 are proved. red 7b490db, green ae26dac |
| F2 | verified: yes | action: fixed | reproduced: applied schema.sql to a fresh database, then `UPDATE vocabulary_scopes SET subsystem_id='B'` moved the row and `DELETE` removed it, both exit 0. vocabulary_scopes_is_immutable and vocabulary_scopes_cannot_be_deleted now ship beside the table, and the gate's required list names it, so C38 holds for every table the specification adds. The red commit is the sharpened gate failing on the unchanged schema for that table alone. red 7b490db, green ae26dac |
| F3 | verified: yes | action: fixed | reproduced: GATE CF1's new B4a handed archivedStoreId a live pre-identity store through an ordinary path and it returned store-legacy-6250723eca5595a1 with no refusal. The legacy derivation is now reached only through `{ immutable: true }`, which opens `file:<path>?immutable=1` through node:sqlite — better-sqlite3 11.10.0 cannot open a URI — and the tuple that open returns must equal the tuple the live read-only open sees, so a -wal carrying a change to git_state is refused rather than named. readArchivedStore forwards it and dev/rebuild-self-conspectus-store.mjs declares it at both archive call sites. The real archive derives store-legacy-36f2623f4d9afe92 immutably and is refused through a plain path. red dd644c5, green c8c9b5b |
| F4 | verified: yes | action: fixed | reproduced: GATE CF1's new F4 arm called carry_finding with a 9005-character archived_finding_id against HEAD's carried.ts and it was accepted; the drop loop's `page.length > 1` then served the last row whatever it cost. carry_finding bounds the id at 200 characters, naming the field and the envelope, and list_carried_findings cuts a single over-budget record down — symptom emptied, id truncated — publishing `compacted: 1` rather than dropping the row, so a store written before the bound stays listable. green b001f22, with the red shown by reverting carried.ts to HEAD and re-running; the revert was undone |
| F5 | verified: yes | action: fixed | reproduced: a store whose only scope_reconciliations row at the revision records tree_digest `bad-tree` and ledger_digest `bad-ledger` reported clause 1 satisfied, because clause 1 re-derived the ledger-against-tree comparison itself and never read the reading it was standing on. Clause 1 now evaluates §3.3's five conditions first — R from git_state.last_checked_sha, resolved with rev-parse; a row at it; last_checked_sha normalizing to it; both digests re-derived from this file's own restatement of §3.2's formula — and then §3.3a, where nonzero unledgered or absent is clause 1 false. The assertion ships a control reconciled by the real detect_changes that holds before and after. red ff24238, green bd107a3 |
| F6 | verified: yes | action: fixed | reproduced: a terminal disposition whose only attachment named deadbeefdeadbeefdeadbeefdeadbeefdeadbeef reported clause 3 satisfied, because the clause counted attachments. Clause 3 now joins disposition_evidence to evidence and requires at least one attachment whose ref_sha resolves, probed in one `git cat-file --batch-check` over the distinct revisions, and emits concern:<sid>/<code>:evidence-revision when none does. §7.3's B3 states the same predicate. The control adds one resolvable attachment and the clause holds. red ff24238, green bd107a3 |
| F7 | verified: yes | action: fixed | reproduced: `cd mcp-server && node test-residual-hardening.mjs` exited 1 with `carried_finding_outcome is neither probed nor declared as a non-input`. The gate now loads dist/tools/carried.js into TOOL_SETS and probes record_carried_outcome.outcome. The probe then found the validator unreachable — requireActiveSession and readCarried both ran before requireEnum — so the handler validates its arguments before it reads the store. GATE P21 green. green 0bf396f |
| F8 | verified: yes | action: fixed | reproduced by reading run.sh:223-233: make_snapshot builds the reviewer's tree with `git archive HEAD` into a fresh `git init`, so the snapshot holds exactly one commit and every regression resolving a historical object fails there. All four commands exit 0 in this worktree, which is what localises the fault to the snapshot. make_snapshot now calls snapshot_history, which fetches b8b566f, d6dd421, 7c1c1a9 and dee59d3 from the worktree by resolved sha, each only after `merge-base --is-ancestor <sha> main` confirms it is not lane work. Verified on a scratch repository: all four objects present, `git log --all` still one commit. The change is in the launcher at ~/.claude/automations/amanuensis-survey-depth/run.sh, outside this repository, so it is not in this branch's commits |
| F9 | verified: no | action: rejected | ran the exact listed command in the worktree: `node dev/test-amanuensis-pecia-defects.mjs` exits 0 — `Amanuensis→Pecia defect gates: 8/8 passed`. No UV cache error, with no UV_CACHE_DIR set. The failure belongs to the reviewer's sandbox rather than to the test or the packet list, so there is nothing here to repair |
