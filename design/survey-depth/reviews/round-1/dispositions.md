# Round-1 dispositions

One row per item in `aggregate.md`. `verified:` records whether this session reproduced the
reviewer's evidence against the repository, the schema, a store opened read-only, or the cited
document. `action: applied` requires `verified: yes`. Where the two reviewers disagreed about the
same claim, the note names the evidence that decided it.

Checks run from the worktree `/Users/nfeldman/automation/claude/amanuensis-survey-depth` at
`ec11d3f`. Stores opened read-only: baseline `…/amanuensis-clean-slate/archive/store-7c1c1a9/memory.db?immutable=1`,
candidate `~/repos/amanuensis/.amanuensis/memory.db?mode=ro`, AxiomDB `~/repos/axiomdb/.amanuensis/memory.db?mode=ro`.

| item | verified | action | what was checked, and what changed |
|---|---|---|---|
| C1/codex | verified: yes | action: applied | counted spec.md §1.2 — D1..D12 is twelve statements, thirteen SQL bodies (D9 has two); claims.md C1 said "ten"; spec §1.6 lists §5's finding accounting as blocking beside the D-measures. C1 now says twelve, and §1.2 names §7.3's B1-B6 as acceptance predicates the gate also evaluates, not measures. |
| C1/claude | verified: yes | action: applied | same recount; also confirmed B3 needs dispositions JOIN subsystems with ref_sha resolution, B4 needs vocabulary_declinations, B5/B6 need carried_findings — none expressible as a D-measure. "executes exactly those and no others" is gone from C1 and §1.2. |
| C2/codex | verified: yes | action: applied | spec.md §1.2 D6 and D7% are per-disposition ratios, not tracked-path fractions; contracts/conspectus-vocabulary.json marks deferred-with-reason obligation_bearing true while spec §1.2 hard-coded it exempt. §1.2 now derives the exempt set from the contract's obligation_bearing field and C2 is limited to tracked-file coverage fractions. |
| C9/codex | verified: yes | action: applied | spec §2.3's predicate is "at least one disposition_evidence row whose ref_sha resolves"; claims.md C9 read "or has one whose ref_sha does not resolve", which refuses a mixed good/bad set. §2.3 now states the at-least-one rule explicitly and reports unreachable attachments separately without refusing. |
| C10/codex | verified: yes | action: applied | src/db.ts:46-58 runs runMigrations then initializeSchema on every openDatabase; :84-89 re-execs schema.sql; migrateVocabularyChecks rebuilds CHECK-constrained tables on existing stores. §2.4's "no store is upgraded in place" overstated. §2.4 now says existing domain rows are not rewritten and acknowledges additive schema migration; new packet P0 tests it on an AxiomDB copy. |
| C11/codex | verified: yes | action: applied | spec §3.2 DDL carries no triggers and §8.2 SR1's red list had no update/delete case. §3.2 gains the trigger pair and §8.2 gains update and delete sabotage cases. |
| C11/claude | verified: yes | action: applied | src/tools/git.ts:239 DELETE FROM scope_gaps then re-insert confirmed; scope_gaps is 0 in both stores against 501 candidate unledgered paths; set_git_state's own description says "Subsequent calls may update any subset of fields", so last_checked_sha alone cannot stand for a reconciliation; schema.sql:2622-2627 is the *_is_immutable / *_cannot_be_deleted pair among 269 triggers. Both reviewers located the same gap; the named triggers are what was applied. |
| C12/codex | verified: yes | action: applied | src/tools/git.ts:220-222 reads git ls-files, the mutable index, not git ls-tree at R. §3.2 gains detected_tree_sha and a path-set witness hash; §3.3 resolves R to a full SHA, enumerates the tree at R, and invalidates standing when the ledger's path set changes. |
| C13/codex | verified: yes | action: applied as modified | ADR-0001:19 clause 1 requires exactly one assignment or explicit exclusion per tracked path; spec §3.3 admitted mapped and publication with a correctly recorded nonzero unledgered count. Applied at the point ADR-0001 binds: publication and the fully-surveyed predicate require zero unresolved gaps; per-subsystem mapped keeps the weaker predicate, because README §4 and decisions.md §1 require a rebuild to progress subsystem by subsystem and clause 2, not clause 1, is the per-subsystem clause. |
| C14/codex | verified: yes | action: applied | spec §3.2's DDL stores six integer counts and no path set, while D2 is a tracked-set intersection and file_ledger classifications can change after the row is written. §3.2 gains the witness hash; §3.3 gains invalidation on ledger mutation before any coverage fraction renders. |
| C15/codex | verified: yes | action: applied | src/helpers.ts:180-207 requireWorkspaceCitation checks indexOf(":"), lastIndexOf("@") and path syntax only — no git, no tree lookup; src/tools/vocabulary.ts:20 required is ["term","gloss"], so first_seen and ref_sha stay optional. §4.2 now separates parsing from resolution and says an unanchored term may be stored but discharges nothing. |
| C16/claude | verified: yes | action: applied | candidate SELECT COUNT(*) FROM vocabulary is 0 across 8 mapped subsystems; §4.3's DDL shipped prose append-only with no trigger while §8.3 VD1 is red "when a declination is updatable or deletable". §4.3 gains vocab_declination_is_immutable and vocab_declination_cannot_be_deleted. |
| C17/codex | verified: yes | action: applied | §4.4's prerequisite is "at least one vocabulary_declinations row" with no recency or resolvability condition, while an anchored term must resolve. §4.4 now requires the effective declination's ref_sha to still resolve; older rows stay as history and cannot discharge a later pass. |
| C17/claude | verified: yes | action: applied | schema.sql:406 is term TEXT PRIMARY KEY and src/tools/vocabulary.ts:36 upserts subsystem_id = COALESCE(excluded.subsystem_id, vocabulary.subsystem_id), so re-defining a shared term for B moves it off A. §4.4 now scopes per (term, subsystem) and define_term refuses to re-scope a term already discharging another subsystem. |
| C18/codex | verified: yes | action: applied | §4.3 lets a term be defined after a declination and §4.5 named no winner. §4.5 gains effective-state precedence: current anchored terms render, superseded declinations render as history. |
| C20/codex | verified: yes | action: applied as modified | the derivation over git_state is not immutable — set_git_state can change last_checked_sha, so one store changes identity, and two rebuilds sharing the tuple collide; both reproduced against the two stores' git_state rows. Applied codex's minted-identity mechanism, not a bare UUID: §5.3 mints store_generation at schema creation, and the frozen git_state digest survives only as the legacy fallback for the existing archive, which has no such row and cannot be written. |
| C20/claude | verified: yes | action: applied | the export's top-level keys are exactly anchor, counts, exported_at, findings, open_questions, source, subsystems; substring tests over the whole file returned False for repo_id, canonical_branch, onboarding_sha, last_checked_sha, git_state and archived_store_id; its anchor 7c1c1a9f… is not the archived store's git_state row default / main / b8b566f / 61bc6b5c…. §5.3 makes archived_store_id an export-contract field and §5.8 carries from the archived store, which does hold all 22 findings with every field carried_findings needs. |
| C21/codex | verified: yes | action: applied | export keys reproduced as above; §5.4 pre-recorded archived terminal rows as repaired before any survey, against §5.4's own current-evidence rule; §5.2 had no table for a carry run or a reasoned empty carry. §5.2 gains carry_runs and §5.4 gains the archived-terminal outcome. |
| C22/codex | verified: yes | action: applied | §5.4 names a carried_finding_evidence join that §5.2 never declares, and "at or after" was undefined on a DAG. §5.2 gains the join DDL; §5.4 defines post-repair as repaired_sha being an ancestor of, or equal to, the attached evidence's revision. |
| C22/claude | verified: yes | action: applied | src/invariants.ts:422-447 is requireOverturnEvidence as quoted; ADR-0001:42-44 and :46-48 are §Resolved and §Verified-fixed; the export's resolution_state histogram is open 13, verified-fixed 8, fixed-pending-verification 1, so 8 rows would have reached repaired with no commit checked. The pre-record is now archived-terminal, which is terminal for the Pecia resolver and is not one of the three authority-bearing outcomes. |
| C23/codex | verified: yes | action: applied | dev/check-living-conspectus.mjs:723 evaluates a supplied manifest via evaluateConspectus(manifest), not a live store. §5.5 now requires a store-scoped evaluation and §7.3 B6 enumerates every carried row from the store or the receipt; a nonempty-undecided sabotage case is added to §8.4. |
| C23/claude | verified: yes | action: applied | same call site; its default fixture is dev/conspectus/self-baseline.json, the immutable A0 fixture design/reader-lenses/spec.md §12.3 forbids retargeting, and dev/test-living-conspectus.mjs:8-15 imports evaluateConspectus over that same fixture; grep for fully.surveyed in mcp-server/src/ returns only vocabulary.ts:1296, a word-list entry. §5.5 names the new store-scoped implementation as a P4 deliverable. |
| C24/codex | verified: yes | action: applied | carried_findings is UNIQUE (archived_store_id, archived_finding_id) while §5.7 step 2 matched on archived_finding_id alone. §5.7 now requires a store-qualified reference and defines an ambiguous match as exit 2, a refusal, never an arbitrary pick. |
| C25/codex | verified: yes | action: applied | the export carries the six lost findings and the anchor but none of the store-identity inputs, reproduced as in C20/claude. §5.8 step 1 now carries from the archived store; the export is accepted only once it carries archived_store_id under the new contract. |
| C26/codex | verified: yes | action: applied | references/phase-1-scope.md:58 calls define_term with first_seen and ref_sha and was absent from §6.1's eight-entry register; phase-2-structural.md:24 and phase-3-concerns.md:120 are two more. §6.1 gains all three and §6.2 requires the register to be reconciled against an inventory of affected call sites. |
| C29/codex | verified: yes | action: applied | §7.3 B1 required only that the recorded unledgered count equal the tree-derived count, so a correctly recorded 501 passed. B1 now requires zero unledgered and zero absent at the acceptance revision plus exactly one assignment or reasoned exclusion per tracked path, which is what the rebuild claims when it claims fully surveyed. |
| C31/codex | verified: yes | action: applied | §7.5's receipt carried verdicts, aggregate counts and per-subsystem attached counts; B3 needs per-disposition evidence SHAs and B4 needs per-subsystem anchor or declination witnesses, neither of which is recorded. §7.5 now records row-level witnesses and §7.1's receipt arm recomputes each predicate instead of reading a verdict field. |
| C32/codex | verified: yes | action: applied | §8.1, §8.2, §8.3, §8.4, §8.5, §8.6, §8.7 and §8.9 each name a control; §8.8 A1 named none. §8.8 gains a valid-receipt control and a seeded forged-green receipt that must turn it red, and §8 requires every gate specification to name its control. |
| C33/codex | verified: yes | action: applied | dev/test-rebuild-readback.mjs:541-552 and :686-699 spawn the rebuild driver with no --carry-from and assert exit 0 at :584. The widening is recorded in §8.10, the test moves into P4's deliverables, and the driver arm passes an explicit reasoned empty carry while a separate case keeps the missing-argument refusal red. |
| C33/claude | verified: yes | action: applied | dev/test-rebuild-depth.mjs:104 is the CHECKLIST_CONCERNS literal and :19-24 states the GP24 property; running the gate here printed GATE P19 RED with 8 failed assertions including the two quoted FAILs and B-02/AL-1 carries no attached evidence row; P8's deliverables listed both rebuild-depth-receipt.json and rebuild-coverage-receipt.json, whose repository_sha is dee59d3e… and whose packet is P17. Decided for claude over codex on this point: codex proposed only to document the widening, but the coverage receipt named as the checklist's new home is a document the same packet rewrites, so the checklist moves to mcp-server/contracts/concern-checklist.json and the coverage receipt leaves P8's deliverables. |
| D1/codex | verified: no | action: rejected | ran the reviewer's own check in the worktree: `test -e design/survey-depth/README.md` exits 0, `git ls-tree HEAD design/survey-depth/ --name-only` lists README.md, and `git log --follow` shows it committed in fb9f1c4, the lane's first commit, and present at 2ca240c. The binding input is in the repository; the reviewer's history-free snapshot did not carry it. |
| D2/codex | verified: yes | action: applied | AxiomDB read-only: 295 file_ledger rows over 187 distinct paths with 53 paths owned by more than one subsystem, 1718 scope_gaps rows, 166 dispositions. P2's acceptance accepted any matching unledgered count. §8.2 SR1 now seeds immutable-tree enumeration, full-SHA normalization, duplicate ownership, a correct-but-nonzero gap, index/tree divergence and a stale reconciliation after ledger mutation. |
| D3/codex | verified: yes | action: applied | §8.3 VD1's red list had an unresolvable-revision case but no absent-path case, and requireWorkspaceCitation does no tree lookup (src/helpers.ts:180-207). VD1 gains missing-path, malformed-SHA, mismatched-anchor and unreachable-revision cases plus the valid-anchored control. |
| D4/codex | verified: yes | action: applied | export keys reproduced; §5.4's pre-recorded repaired outcomes and the unspecified --carry-from none reason both confirmed in the spec text. §5.2 gains carry_runs, §5.3 gains store_generation, §5.4 gains --carry-reason and separates archived from current outcomes. |
| D5/codex | verified: yes | action: applied | §5.4 names carried_finding_evidence; §5.2 declared two tables; §7.4 step 6 promises a third-process carried-count read-back with no tool to call. §5.2 gains the join DDL and §5.2a specifies attach_carried_evidence and list_carried_findings with a page size and a byte budget; P4's acceptance gains the third-process read-back. |
| D6/codex | verified: yes | action: applied | scripts/gen-tool-inventory.mjs reconcile() reports a tool "declared in <file> but tools/list does not advertise it"; DEVELOPMENT.md's generated block runs :200 to :615; src/index.ts:80 and :105 import the tool arrays and :234 and :237 spread them. DEVELOPMENT.md added to P3 and P4; src/index.ts added to P4; an inventory sabotage case added to P3's and P4's acceptance. |
| D7/codex | verified: yes | action: applied | §5.6 requires carried obligations on Unresolved and History; P6 depended on P2 and P3 only, no packet listed both a renderer file and carried records, and readback.py:252-262 censuses findings, stale entries and the locus index but no carried rows. P6 now depends on P4, delivers the carried markers in both formats, and gains the state, coverage and content sabotage cases. The reviewer's accessibility clause — semantic headings, keyboard navigation, a complete no-JavaScript path — is not applied: it is asserted without evidence that the current pages lack it, and this lane changes no page structure. |
| D8/codex | verified: yes | action: applied | §7.5's receipt as written cannot supply B3's evidence revisions or B4's anchors, so A1 could validate a self-reported green. Same fix as C31: row-level witnesses recorded and recomputed, plus §8.8's seeded forged-green receipt. |
| D9/codex | verified: yes | action: applied | contracts/conspectus-vocabulary.json declares deferred-with-reason obligation_bearing true; spec §1.2 listed it among the exempt classifications; AxiomDB holds 35 such paths. §1.2 derives the exempt set from the contract and P5's acceptance gains a deferred-path regression. Neither store's histogram contains a deferred-with-reason row, so no number in §1.3 changes. |
| D10/codex | verified: yes | action: applied | dev/test-rebuild-readback.mjs spawns the driver at :541-552 and :686-699 with no --carry-from. Added to P4's deliverables with an explicit empty carry in the positive controls and a retained missing-argument red case. |
| D11/codex | verified: yes | action: applied | §6.2 admits the register cannot turn red for an omitted refusal, and phase-1-scope.md:58 is an already-unregistered affected caller. §6.2 now derives the candidate refusal set from the server's own thrown messages and compares that complete set against the register, so an unregistered refusal is a finding rather than silence. |
| D12/codex | verified: yes | action: applied | P1, P2, P3 and P4 all had depends_on [] and all four list mcp-server/src/invariants.ts; P2, P3 and P4 also share src/schema.sql and src/db.ts, and P3 and P4 share contracts/conspectus-vocabulary.json. S1 is now serialized P0 to P1 to P2 to P3 to P4. |
| D13/codex | verified: yes | action: applied | AxiomDB read-only: 166 dispositions, 54 unattached, 35 of 35 subsystems mapped, 1718 scope_gaps, 41 vocabulary rows, and neither scope_reconciliations nor carried_findings present in sqlite_master. New packet P0 copies the store to temporary storage and gates additive migration, refusal behaviour, non-mutation of existing rows and clean-publish rollback. |
| D14/codex | verified: yes | action: applied | P8 combined reinitialization, a whole-repository survey, 22 adjudications, publication, promotion, three receipts and regression repair in 300 minutes. Split into P8 carry and onboarding, P10 batched subsystem survey and carried adjudication, P11 publication and receipts. |
| D15/codex | verified: yes | action: applied | same finding as C32/codex, verified the same way: §8.8 is the only gate section with no Control sentence. A1 gains the valid bound-receipt control and the paired forged-receipt failure. |
| D16/codex | verified: yes | action: applied | P8's regression list held test-invariants, test-derived-staleness, test-smoke, the materializer gates and the rebuild gates, and none of SD1, SR1, VD1, CF1, PM1, RP1 or CF2. All seven now run inside the rebuild packets before their acceptance. |
| D1/claude | verified: yes | action: applied | grep -rln set_disposition over mcp-server/*.mjs and dev/*.mjs returns 9 files: test-invariants 19 refs, test-residual-hardening 12, test-perf-ceilings 3, test-vocabulary-source 2, test-perf-tier2 2, test-consumer-route 2, test-cloud-e2e 2, test-smoke 1, dev/test-rebuild-regeneration 1. P1 named none of them as deliverables and omitted five from its regression list. All nine are now P1 deliverables, the five missing commands are in its regression list, and budget_minutes rises from 150 to 240. |
| D2/claude | verified: yes | action: applied | mcp-server/test-perf-ceilings.mjs:187 is ceiling("set_disposition (resolves ref_sha)", 200, …) and the call at :189-200 passes no evidence_ids; the comment at :171-184 records 6.35-6.50 ms measured against a 200 ms ceiling, and src/helpers.ts:124 says the subprocess "is paid on every durable write". §2.2 step 2 now resolves the distinct ref_sha values of the named rows in one batched git cat-file --batch-check, and the ceiling is in P1's regression list. |
| D3/claude | verified: yes | action: applied | baseline SELECT COUNT(*), MIN, MAX FROM sessions is 35 rows spanning 2026-08-12 04:19:14 to 2026-09-11 23:05:07 for 221 examined paths; the candidate is 6 sessions spanning 2026-09-14 03:22:59 to 05:50:37 for 84. B2 at D2=605 needs at least 361 examined. P8 is split and each part is sized from the baseline's measured cost. The reviewer's "98 minutes" is report.md's figure; the store's own session span is about 148 minutes, which does not change the conclusion. |
| D4/claude | verified: yes | action: applied | plan.json P4 deliverables contain dev/pecia-resolve-finding.mjs and P4's acceptance says it "follows the §5.7 lookup order", while every red condition of GATE CF2 (§8.9) is a property of that script and none names a P9 deliverable. dev/test-carried-finding-references.mjs moves to P4; P9's gate now asserts that dev/pecia-dogfood.md accounts for each of the eight pc-* records, which report.md names as pc-1a91, pc-207e, pc-707e, pc-80b8, pc-833d, pc-adce, pc-ae87, pc-d688. |
| D5/claude | verified: yes | action: applied | decided together with C33/claude and C33/codex; see that row. The coverage receipt is out of the rebuild packets' deliverables and the checklist moves to mcp-server/contracts/concern-checklist.json with a --check. |
| D6/claude | verified: yes | action: applied | dev/test-rebuild-readback.mjs:541-554 spawns the driver with --confirm --workspace --archive --receipt and no --carry-from, and :494-500 records why the gate drives the real script. Same fix as D10/codex, plus an assertion that the empty-carry record was written so the --carry-from none branch is not the untested branch. |
| D7/claude | verified: yes | action: applied | src/index.ts:80 and :105 import dispositionTools and vocabularyTools and :234 and :237 spread them into the tool array; P4 introduced src/tools/carried.ts without listing index.ts. Added. |
| D8/claude | verified: yes | action: applied | DEVELOPMENT.md's generated block is :200 to :615 and gen-tool-inventory.mjs --check exits 1 if it would change; P3 and P4 both run that check and DEVELOPMENT.md was only in P1's deliverables. Added to both. |
| D9/claude | verified: yes | action: applied | scripts/gen-vocabulary.mjs:28-31 writes mcp-server/src/vocabulary.ts and materializer/amanuensis_materializer/vocabulary.py; neither was a deliverable of any packet. The contract already declares finding_resolution_state with exactly the five values §5.2's comment lists. Both files added to P3 and P4; carried_finding_outcome is declared in the contract and archived_resolution takes its CHECK from finding_resolution_state. |
| D10/claude | verified: yes | action: applied | 517 statements in src/schema.sql begin with CREATE and 516 carry IF NOT EXISTS; src/db.ts:85-88 states the schema is written that way "so we can run it on every open" and :89 execs it; schema.sql:2622-2627 is the immutability pair among 269 triggers. All four DDL blocks in §3.2, §4.3 and §5.2 are rewritten with IF NOT EXISTS and each table gains its trigger pair, and P2, P3 and P4's acceptance says the trigger is what makes the table append-only. |
| D11/claude | verified: yes | action: applied | P8's regression list contained node dev/test-rebuild-depth.mjs and node dev/test-survey-depth.mjs and P9's contained both, while spec §8.10 asserts "Neither appears in any packet's regression list". Both removed from the rebuild packets' regression lists and §8.10's sentence corrected. The reviewer quoted 10 failed assertions; running the gate here prints GATE P19 RED with 8, which is the count §8.10 already states and does not change the finding. |
| D12/claude | verified: yes | action: applied | renderers.py:415 computes obligation_files as SUM over file_ledger rows only and :630 reads it, so the new D2 denominator differs from today's by exactly the unledgered count. P6's control is restated as two fixtures: zero-unledgered asserted byte-for-byte against today, and unledgered greater than zero asserted against the new denominator with the old value recorded in the gate header as the intended change. |
| D13/claude | verified: yes | action: applied | plan.json completion.commands holds 97 entries and none matches record-survey-depth-baseline; no packet's regression list contains it either; §7.2's archive path is a machine-local absolute path outside the repository. The --check is added to P5's regression list and to completion.commands, and §7.2 requires it to exit 2 reporting cannot run, never green, when the archive is absent. |
| D14/claude | verified: yes | action: applied | §7.1's candidate arm reads the live store "and otherwise the committed acceptance receipt", which is a rebuild-packet deliverable, while P5 registers the gate in CI and GATE D1 is red when it is absent from CI. §7.1 gains an explicit cannot-run exit 2 for no live store and no committed receipt, and §8.5 asserts that state is reached rather than green. |
| D15/claude | verified: yes | action: applied | src/db.ts:44 is REQUIRED_VIEWS = ["file_standing", "finding_state_current"] and :61-82 only asserts those views exist; new tables arrive through initializeSchema re-running schema.sql at :84-89, and runMigrations handles columns and indexes. P2's last acceptance bullet is restated in those terms. |
