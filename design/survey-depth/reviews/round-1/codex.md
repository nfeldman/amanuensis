## Claims

| Claim | Verdict | Evidence | Proposed change |
|---|---|---|---|
| C1 | overturned | `spec.md:50-112` defines twelve measures, D1–D12, while `spec.md:758-775` adds six distinct blocking predicates B1–B6. | Say “twelve frozen measures”; distinguish measurement queries from additional acceptance predicates. |
| C2 | qualified | `spec.md:82,90` defines D6 and D7% over dispositions, not tracked paths. Also `mcp-server/contracts/conspectus-vocabulary.json:224-228` marks `deferred-with-reason` obligation-bearing, while `spec.md:64-66` exempts it. | Limit the rule to tracked-file coverage fractions; derive exemptions from the single-source `obligation_bearing` field. |
| C3 | upheld | The baseline and candidate name different checked revisions; the corresponding trees contain 503 and 607 paths. | |
| C4 | upheld | Read-only baseline queries returned 503 distinct ledger paths, 128 dispositions, 112 attached dispositions, 13 open findings, and zero scope gaps, matching the claim. | |
| C5 | upheld | Read-only candidate queries reproduced the stated 106 ledger rows, 84 examined paths, 180 dispositions, 3 attached dispositions, and 14 open findings. | |
| C6 | upheld | `decisions.md:11-16` requires per-record discharge without numeric floors; `spec.md:182-199` correctly separates blocking and reported axes. | |
| C7 | upheld | `dispositions.ts:24-118` currently omits attachments and `evidence.ts:68-102` is the separate writer; the proposed transactional contract closes the demonstrated gap. | |
| C8 | upheld | The claimed quality is already defined as the strongest attached evidence kind, and the canonical ladder exists in generated vocabulary. | |
| C9 | qualified | `spec.md:264-265` requires at least one resolvable attachment, but the claim’s “has one whose ref_sha does not resolve” can mean any bad attachment, including a mixed good/bad set. | State explicitly that a disposition passes when at least one attachment resolves; separately report unreachable attachments. |
| C10 | qualified | `mcp-server/src/db.ts:46-58,84-100` runs migrations and schema initialization on every existing store; `:137-192` can backfill and rebuild tables. P2 also requires additive table creation on existing stores. | Say that existing domain rows are not rewritten for these obligations; acknowledge additive schema migration and test it separately. |
| C11 | qualified | The proposed DDL in `spec.md:324-337` has no update/delete triggers, and SR1 at `spec.md:849-855` does not test immutability. | Enforce append-only behavior with triggers and add update/delete sabotage cases. |
| C12 | overturned | `git.ts:215-240` derives counts from mutable `git ls-files`, not revision R’s immutable tree. The three-condition predicate also survives later ledger mutation and does not prove complete assignment. | Resolve R to a full SHA, enumerate `git ls-tree` at R, store a path-set witness/hash, and invalidate standing when relevant ledger state changes. |
| C13 | overturned | ADR-0001 requires every tracked path to have exactly one assignment or explicit exclusion, but `spec.md:350-368` permits mapped/publication with correctly recorded nonzero `unledgered` or `absent` counts. | Require zero unresolved scope gaps and exactly-one assignment-or-exclusion before mapped or publication. |
| C14 | qualified | `scope_reconciliations` stores only aggregate counts. D2 requires tracked-set intersections, and live ledger classifications can change after the recorded reconciliation. | Use a witnessed path set or hash and invalidate/reconcile after ledger mutations before rendering numeric coverage. |
| C15 | overturned | `spec.md:423-430` calls `requireWorkspaceCitation`, but `helpers.ts:180-208` checks delimiters and path syntax only; it does not resolve the embedded revision or prove the path exists there. `first_seen` and `ref_sha` also remain optional. | Parse the canonical citation grammar, resolve its revision, verify the path in that tree, and define when unanchored terms may be stored but cannot discharge the gate. |
| C16 | upheld | `decisions.md:11-13` expressly permits reasoned declination instead of a numeric term quota; the proposed separate historical record preserves that judgment. | |
| C17 | qualified | `spec.md:446-449` preserves declinations across resets, while the proposed prerequisite accepts any declination without rechecking its revision or effective standing. | Require the effective declination’s revision to remain resolvable; preserve older rows as history without letting stale history silently discharge a new pass. |
| C18 | qualified | `spec.md:446-453` permits a term to be defined after a declination, so both records can coexist, but `spec.md:477-488` does not define which current rendering wins. | Define effective-state precedence: render current terms when present and show superseded declinations as history. |
| C19 | upheld | `findings.finding_id` is a single-store namespace; separate carried tables avoid collisions and false attribution to the successor survey. | |
| C20 | overturned | The proposed hash identifies a mutable git-state tuple, not a store generation: one store changes identity when `last_checked_sha` changes, while two rebuilds with the same tuple collide. The accepted JSON export does not contain that tuple. | Persist a random immutable store-generation UUID and include it in every archive/export and carried record. |
| C21 | overturned | The export’s keys are only `anchor`, `counts`, `exported_at`, `findings`, `open_questions`, `source`, and `subsystems`, so its store ID cannot be recomputed. Also `spec.md:560-564` pre-records archived verified-fixed rows as repaired before survey, contradicting the current-evidence requirement at `:575-578`; no table records a reasoned empty carry. | Add a carry-run manifest with source identity, reason, expected/imported counts, and imported archival states; leave all current-store outcomes undecided until current authority rules are met. |
| C22 | qualified | `spec.md:571-578` names a `carried_finding_evidence` join but supplies no DDL or attachment contract, and “at or after” is undefined on a Git DAG. | Specify the join/API and define post-repair as `repaired_sha` being an ancestor of the attached evidence revision. |
| C23 | qualified | `dev/check-living-conspectus.mjs:238-466` evaluates a supplied manifest, not the live store; the plan does not define a DB-to-manifest carried-record census. | Require the whole-store adapter to enumerate every carried row from the store or receipt and add a nonempty undecided sabotage case. |
| C24 | qualified | `carried_findings` is unique by store plus archived ID, but `spec.md:621-626` resolves using archived ID alone. Multiple archives may therefore produce ambiguous matches. | Require a store-qualified reference or define ambiguity as a refusal rather than choosing an arbitrary carried record. |
| C25 | qualified | The export contains the six findings and anchor, but lacks the store-generation identity required by C20. | Extend or pair the export with authenticated generation metadata before using it as the retroactive source. |
| C26 | qualified | `phase-1-scope.md:58` also invokes changed `define_term` behavior but is absent from `spec.md:663-672`; coordinator and packaging call sites are likewise outside the eight-entry register. | Generate an affected-caller inventory and update every caller that can encounter a new refusal. |
| C27 | upheld | The mechanism is accurately described as narrow string identity, and `spec.md:697-704` explicitly records its unregistered-refusal false green. | |
| C28 | upheld | The proposed committed baseline arm avoids the existing live-store zero-denominator failure and explicitly treats an unreadable baseline as red. | |
| C29 | overturned | B1 at `spec.md:762-764` requires the recorded unledgered count merely to equal the tree-derived count; a correctly recorded value of 501 passes despite ADR-0001’s complete-inventory requirement. | Make B1 require zero unledgered and absent paths plus exactly one assignment or reasoned exclusion per tracked path. |
| C30 | upheld | The worktree has no `.amanuensis` directory, and the unconditional empty snapshot/delete exercise is a valid control of the rebuild branch. | |
| C31 | overturned | `spec.md:816-824` records verdicts and aggregate counts, but B3 requires per-disposition attachment SHAs and B4 requires term-anchor or declination witnesses. Those predicates cannot be independently recomputed from the stated receipt. | Store canonical row-level witnesses and hashes for every blocking predicate; recompute verdicts rather than trusting recorded verdict fields. |
| C32 | overturned | Gate A1 at `spec.md:931-940` states a red condition and false green but names no must-stay-green control. | Add a valid-receipt control and require every gate specification and test to exercise its control. |
| C33 | overturned | `dev/test-rebuild-readback.mjs:541-552,686-699` invokes the rebuild driver without `--carry-from`; making that option mandatory breaks this existing gate, yet P4 does not list the test as a deliverable to update. | Update the gate to pass an explicit reasoned empty carry while retaining a separate missing-argument refusal test; document this as a widening. |

## Plan defects

| Id | Severity | Location | Evidence | Proposed change |
|---|---|---|---|---|
| D1 | high | `spec.md:4-6` binding inputs | `test -e design/survey-depth/README.md` failed; the directory contains only claims, decisions, plan, and spec files. Numerous claims rely on that absent proposal. | Commit the binding proposal or remove it as a binding input and reproduce every governing requirement in present artifacts. |
| D2 | high | P2 gate SR1 and acceptance | P2 re-derives from mutable `git ls-files` and accepts any matching unledgered count. AxiomDB already has 295 ledger rows but only 187 distinct paths and 1,718 unledgered gaps, exposing duplicate ownership and nonzero-gap cases the gate would not reject. | Test immutable-tree enumeration, full-SHA normalization, duplicate ownership, correct-but-nonzero gaps, index/tree divergence, and stale reconciliation after ledger mutation. |
| D3 | high | P3 gate VD1 | VD1 tests unresolved citation revisions but not a citation whose path is absent at that revision; `requireWorkspaceCitation` performs no resolution or tree lookup. | Add missing-path, malformed-SHA, mismatched-anchor, and unreachable-revision red cases plus a valid anchored control. |
| D4 | high | P4 carry contract | The accepted export lacks the four fields needed for `archived_store_id`; pre-recorded repaired outcomes conflict with current evidence; `--carry-from none` has no specified reason argument or durable record. | Add immutable store-generation and carry-run schemas, explicit empty-carry reason syntax, source/import counts, and separate archived versus current outcomes. |
| D5 | high | P4 deliverables and tool contract | `spec.md:571-573` requires `carried_finding_evidence`, but §5.2 and P4 acceptance specify only two tables. No attachment tool or compact paginated read/list tool is defined. The promised third-process carried-count read-back consequently has no named surface. | Specify the join DDL, attachment and reader tools, pagination/byte budgets, response compactness, and third-process read-back test. |
| D6 | high | P3/P4 generated tool inventory | P3 and P4 add tools, but their deliverables omit `mcp-server/DEVELOPMENT.md`; P4 also omits `mcp-server/src/index.ts`. `gen-tool-inventory.mjs:60-80` rejects tools declared but not advertised and checks the generated inventory. | Add registration and regenerated inventory deliverables to both packets; require inventory sabotage tests. |
| D7 | high | P4/P6 projection and read-back | `spec.md:603-610` requires carried obligations on Unresolved and History pages, but P6 depends only on P2/P3 and covers only reconciliation/vocabulary. `readback.py:252-292` independently censuses findings and stale entries, not carried records. | Make P6 depend on P4; implement both-format carried markers, state/coverage/content-axis sabotage tests, semantic headings, keyboard navigation, and a complete no-JavaScript reading path. |
| D8 | high | P5/P8 receipt arm and A1 | The receipt contract contains aggregate attached counts and recorded verdicts, not the evidence revisions, vocabulary anchors, or declination records needed to evaluate B3/B4. A1 can therefore validate a self-reported green receipt. | Record canonical witnesses and independently recompute every predicate; seed a forged-green receipt that must turn A1 red. |
| D9 | high | P5 D2/B2 implementation | `conspectus-vocabulary.json:224-228` makes `deferred-with-reason` obligation-bearing, but `spec.md:64-66` hard-codes it as exempt. AxiomDB contains 35 such paths. | Generate the exemption predicate from the canonical vocabulary contract and add a deferred-path regression. |
| D10 | high | P4 regression `dev/test-rebuild-readback.mjs` | Existing driver invocations at lines 541-552 and 686-699 omit the newly mandatory carry argument. P4 runs this regression but does not deliver its required update. | Add the test to P4 deliverables, pass explicit empty carry in positive controls, and retain a missing-argument red case. |
| D11 | medium | P7 refusal-parity gate | The hand-maintained register cannot turn red for an omitted refusal, as `spec.md:702-704` admits; `phase-1-scope.md:58` is already an unregistered affected caller. | Derive the refusal inventory from structured server declarations and compare its complete ID set with every affected skill caller. |
| D12 | medium | S1 dependencies | P1–P4 all have empty dependencies while P1–P4 overlap `invariants.ts` and workflow files, and P2–P4 overlap `schema.sql` and `db.ts`. They are simultaneously ready in one slice. | Serialize overlapping packets or split shared schema/invariant/CI integration into an explicit dependency packet. |
| D13 | high | Cross-store compatibility | C10 imposes immediate obligations on AxiomDB, yet no packet tests an existing-store copy. Read-only queries found 166 dispositions, 54 unattached, 35 mapped subsystems, 1,718 scope gaps, and no proposed tables. | Copy the AxiomDB store to temporary storage and gate additive migration, refusal behavior, non-mutation of existing rows, and clean-publish rollback. |
| D14 | medium | P8 budget and recovery boundary | One 300-minute packet combines destructive reinitialization, a complete repository survey, 22 independent adjudications, publication, promotion, three receipts, and regression repair. | Split reinitialization/carry, subsystem survey batches, carried adjudication, and final publication/receipt into separately gated packets. |
| D15 | medium | Gate A1 | Unlike SD1, SR1, VD1, CF1, PM1, RP1, and CF2, `spec.md:931-940` names no selective green control for A1. | Add a valid bound receipt control and a paired forged-receipt failure. |
| D16 | medium | P8 regression list | P8 does not rerun new gates SD1, SR1, VD1, CF1, PM1, RP1, or CF2 even though it rebuilds the store, rewrites receipts/docs, and changes CI. They run only at final completion. | Run every new substrate, projection, parity, and receipt gate inside P8 before accepting its rebuild. |

## Notes

The snapshot cannot resolve `61bc6b5`, `7c1c1a9`, or `d2b1630`; tree counts were independently verified against the read-only primary checkout as 503, 607, and 599.
`node dev/test-rebuild-readback.mjs` currently exits 1 here because the history-free snapshot cannot resolve the committed receipt’s revision.
The Amanuensis skill’s evidence hierarchy and claim-challenge discipline materially guided the review; no repository files were modified.