# Fix dispositions — slice-S2

Run `20260914-214250-fix-slice-S2`, one reviewer (codex), so the `/name` suffix is omitted.
Every finding was reproduced or refuted against the code before anything was changed; no test
or gate lost an assertion.

| F1 | verified: yes | action: fixed | reproduced: disabled the receipt-arm B6 refusal at dev/test-survey-depth.mjs:1045 and D1 exited 0 "GATE P5 GREEN"; D1 had no receipt-arm B6 case. Added one (red-gates.mjs `receipt-forged-b6`, an undecided witness that is not one of the baseline thirteen, so B5 cannot fire instead); with the same sabotage re-applied and undone locally, D1 now exits 1 naming that case. red ffbd2ed, green 17ce9ae |
| F2 | verified: yes | action: fixed | reproduced: emptying `blocking.B3.dispositions` left D0's loop nothing to walk and it printed GATE D0 GREEN. B3 now reconciles its witness table against `reported.dispositions` — the census §7.5 records independently — and requires the (subsystem_id, concern_code) pairs distinct, so padding by repetition fails too. Two D1 cases, both red at ffbd2ed, green at 17ce9ae |
| F3 | verified: yes | action: fixed | reproduced: `ledger_digest` set to 64 zeroes and D0 stayed green; the arm read `detected_sha`, `tree_digest`, `tracked_paths`, `unledgered`, `absent` and never that field. The receipt now carries its (path, classification) ledger rows; B1 re-derives the digest from them, checks `ledger_rows`, and recomputes `unledgered`/`absent` against the tree at the witnessed revision. A truncated-and-rehashed witness is red too. red ffbd2ed, green 17ce9ae |
| F4 | verified: yes | action: fixed | reproduced: a reconciled fixture bound to a nonexistent workspace published `2 of 5` and `2`. readback.py:341 skipped §3.3 condition 4 when `tree_paths` returned None, and condition 1 with it. §3.3 is a conjunction: `reconciliation_standing` now returns non-standing with `WHY_NO_TREE`, and `_unmeasured` gives it its own revision-and-reason sentence. red a9e49c7, green 7c7b877 |
| F5 | verified: no | action: rejected | reproduced the rendering — an unreconciled fixture does print `Files carrying a survey obligation marked stale: 0 of 3` — but it is not a coverage row. It sits in Source alignment, not Survey coverage, and both halves count obligation-bearing *ledger rows* (renderers.py:415, read at :745), which §11.2 measures freshness over. spec §3.4 names the three renderings that become `not measured` and this is not among them; C14 cites this row's existing "not measured by this projection" branch as the model the new refusal copies. The proposed denominator would pair a numerator over ledger rows with a denominator over tracked paths — an unledgered path has no `examined_at` to be stale against — which is the two-universes fraction §1.4 forbids. PM1 control (a) pins the same row at `0 of 5` on a reconciled fixture, so the row counts one universe in both standings |
| F6 | verified: yes | action: fixed | reproduced: `first_seen = "not-a-citation"` still rendered the term as current and the declination as superseded history. `_vocabulary_state` (renderers.py:2278) tested only for a non-empty string, where §4.4 and D0's own B4 require an anchor that opens. Added `anchor_opens` in readback.py and three PM1 cases — an unparseable token, an unresolvable revision, a path absent from that revision's tree. red a9e49c7, green 7c7b877 |
| F7 | verified: yes | action: fixed | reproduced: `2 of 5` rewritten to `999 of 5` read back coverage-green, because `wanted not in cell` matched the substring `of 5`. The same hole sat on the unledgered row (`"2" in "12"`). `_coverage_measurement_census` now compares the cell to the exact reading, with `examined_tracked` recomputing the numerator from the store; a third case asserts the restored projection is green, so neither red is the tampering. red a9e49c7, green 7c7b877 |
| F8 | verified: yes | action: fixed | reproduced: a `git archive HEAD` + fresh `git init` snapshot ran D1 to 27 failed assertions, first "the fixture binds itself to the revision it was measured at". D0's refusal is correct and deliberate (dev/receipt-provenance.mjs header, slice-S6 F1/F2); the fault was D1 handing D0 the committed fixture whose anchor is 61bc6b5. D1 now writes its own baseline — the committed one re-anchored to this checkout's HEAD — for every case not about the fixture, the rebound-store case re-anchors its own forged fixture, and the recorder case is two-sided on whether the archive's own revision is in this checkout (exit 0 where it is, exit 2 `cannot run` where it is not). Verified: D1 GREEN in a history-free snapshot of this tree and GREEN in the full-history worktree. fix e914814 |
| F9 | verified: no | action: rejected | not reproducible in a checkout with history: `node dev/check-living-conspectus.mjs`, `dev/test-rebuild-readback.mjs`, `dev/test-rebuild-coverage.mjs` and `dev/test-reader-lenses-dogfood.mjs` all exit 0 here, and `dev/record-survey-depth-baseline.mjs --check` exits 0. The reviewer's own note isolates it ("P5 passed untouched in the byte-identical full-history lane worktree"). These are provenance checks refusing to certify revisions a discarded history cannot show, which dev/receipt-provenance.mjs documents as the intended third state; the proposed "recognize truncated history consistently" is the zero-denominator green slice-S6 removed. F8 fixes the one command in this set that was D1's own fault rather than the snapshot's |
| F10 | verified: no | action: rejected | not reproducible: `python3 materializer/test-search-index.py` exits 0 here with all 24 assertions green, browser arm included ("GATE P10 GREEN"). The reviewer's environment had no browser, and the gate reported that and turned red rather than passing — the conservative behaviour. The proposed "report the browser arm as explicitly unevaluated when unavailable" would make a gate green on a machine that never ran half of it, which is the false green these gates exist to refuse (VP4(e)) |
