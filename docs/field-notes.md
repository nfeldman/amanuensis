# Leads

_Observations from every survey pass that are not yet findings: patterns, anomalies, connections, tensions, and candidate concerns recorded when a reader noticed something the phase structure did not ask for. Closed leads are on [Resolved leads and questions](resolved-leads.md)._

## Tension

_3 open leads._

- <a id="lead-69"></a>adversarial probe for claim [B-03](subsystems/b03-conspectus-schema-vocabulary-and-invariants.md)/key-type/mcp-server-src-schema-sql-claims superseded it: the reading called the row immutable, and claims.ts executes UPDATE claims SET valid_until_sha on both the invalidation and the supersession path. The probe looked for a second mutable column and found none — the claims_versions_are_immutable trigger aborts any update that touches anything but a still-open valid_until_sha — so the corrected reading is a controlled close-plus-insert, which the successor records. @ `B-03` · _recorded 2026-09-12 14:51 UTC_
- <a id="lead-41"></a>adversarial probe for finding [B04-5](findings.md#b04-5) narrowed its scope. Claim A as Phase 3 implied it: the two definitions of "an open bug" can report different numbers. Claim B: they cannot at this revision, because add_finding and update_finding_status each write findings.status and the resolution event in one transaction, so status='confirmed-bug' and resolution_state='open' are equivalent today. In scope: the schema's stated invariant that every reader of finding resolution selects from the view, which is false, and the third copy of the predicate. Out of scope: any present disagreement between the dashboard count and a subsystem page. The finding stands as scope-restricted; verify_finding_fix already writes an event without touching the column, so the equivalence is a habit of today's writers and not a property of the model. @ `B-04` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-2"></a>Two contracts pull against each other at the reader surface: the response must stay under 8192 wire bytes, and the attention list must reconcile exactly against the store's open findings. They are reconciled by counting the budget-omitted ids into the equality rather than by relaxing either — worth reading before changing either number, because raising the budget and dropping the omission ledger would each break the other contract quietly. @ `mcp-server/src/tools/locus.ts:WIRE_BUDGET@258ccd2` · _recorded 2026-09-12 12:18 UTC_

## Anomaly

_2 open leads._

- <a id="lead-16"></a>migrateVocabularyChecks declines to repair a binding whose canonical CREATE TABLE it cannot locate in schema.sql, and does so silently — the one `continue` in that loop with no comment beside it, where the other three state their reasons. The condition means the contract names a table the schema does not define, which gen-vocabulary.mjs --check-sql catches because it also reads schema.sql; the run-time pass declining without a word is what is worth recording. @ `mcp-server/src/db.ts:migrateVocabularyChecks@0fee11b` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-3"></a>adversarial probe for claim [B-01](subsystems/b01-server-runtime-and-tool-dispatch.md)/key-type/mcp-server-src-helpers-ts-tooldefinition overturned it: the Phase 2 reading recorded a four-member shape with a handler taking (ctx, args), and the declaration has five members with the handler taking (args, ctx). B-01-C01 is invalidated at 0fee11b and B-01-C08 re-asserts the key with the corrected reading, which names the `compact` flag the wire budget depends on. @ `B-01` · _recorded 2026-09-12 12:49 UTC_

## Connection

_1 open lead._

- <a id="lead-57"></a>[B04-5](findings.md#b04-5) and [B08-1](findings.md#b08-1) are one defect at two call sites: list_subsystems and render_master_plan each compute "an open bug" from findings.status while their neighbours join finding_state_current. The pair is worth more than either alone, because it shows the conversion to the view was done reader by reader from a list — the view's own comment enumerates which ones — rather than by making the coarse column unreadable. A repair that converts only the two named here leaves the next reader free to add a third. @ `mcp-server/src/schema.sql:finding_state_current@0fee11b` · _recorded 2026-09-12 13:03 UTC_

## Pattern

_63 open leads._

- <a id="lead-68"></a>adversarial probe for finding [B11-2](findings.md#b11-2) did not find an overturning mechanism, and measured the claim rather than asserting it: `rg --text -n pairKey dev/test-rebuild-coverage.mjs` returns the three real matches, so the content is reachable and the default is what hides it. Node's behaviour is unaffected — a raw NUL and the escape denote the same code point — which is why this is a legibility defect and not a correctness one. @ `B-11` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-67"></a>adversarial probe for finding [B11-1](findings.md#b11-1) did not find an overturning mechanism: the candidate was another job running the same gates with history. Neither dev/test-rebuild-readback.mjs nor dev/test-rebuild-coverage.mjs appears in the roadmap job or in any other; each runs exactly once, in the mcp-server job, at depth 1. @ `B-11` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-66"></a>adversarial probe for finding [B10-1](findings.md#b10-1) did not find an overturning mechanism: the candidate was a second checker reading SKILL.md. test-vocabulary-source.mjs is the only other CI step named for the vocabulary, and it asserts the source's own shape — that every value carries a label, a meaning and a cannot_justify line — rather than comparing it with prose. No other script in the repository reads SKILL.md at all. @ `B-10` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-65"></a>adversarial probe for claim [B-11](subsystems/b11-development-harness-and-gates.md)/concurrency did not overturn it: no CI job reads an artifact another job wrote — there is no upload-artifact or download-artifact step in the workflow — so the jobs are independent by construction rather than by ordering. @ `B-11` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-64"></a>adversarial probe for claim [B-11](subsystems/b11-development-harness-and-gates.md)/state-container did not overturn it: the receipts are read by later runs and written by none of them, and the claim's own qualification — a receipt proves what was true when it was written and nothing about now — is why each gate pairs the receipt arm with a live arm wherever a subject exists. @ `B-11` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-63"></a>adversarial probe for claim [B-11](subsystems/b11-development-harness-and-gates.md)/flow/prove-a-gate/01 did not overturn it: the header states its red conditions and its false greens, and the scrub table routes every emitted line through a replacement pass, so the crash signatures the launcher rejects cannot appear in a message about an absent deliverable. @ `B-11` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-62"></a>adversarial probe for claim [B-11](subsystems/b11-development-harness-and-gates.md)/key-type/github-workflows-test-yml-pecia-defects did not overturn it: the job carries an if: on a repository variable, and once enabled an unset token fails the step with a message rather than skipping — the distinction the claim describes is in the workflow, where a script cannot undo it. @ `B-11` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-61"></a>adversarial probe for claim [B-10](subsystems/b10-the-amanuensis-skill.md)/concurrency did not overturn it: the prose does permit concurrent phase subagents and coordinates none, and the probe's question — whether that is a hazard — is answered outside the prose, by one server process holding one synchronous connection, so two passes serialize at the handle rather than by agreement. @ `B-10` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-60"></a>adversarial probe for claim [B-10](subsystems/b10-the-amanuensis-skill.md)/state-container did not overturn it, and this pass is itself the evidence: every durable thing produced here went through a tool call, and the only state the prose held was in the reading of it. @ `B-10` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-59"></a>adversarial probe for claim [B-10](subsystems/b10-the-amanuensis-skill.md)/flow/survey-a-subsystem/01 did not overturn it: phase-2-structural.md names the five key shapes and defines the slug as the whole path:symbol pair, and claims.ts refuses a file-anchored claim on the same grounds — the prose and the refusal agree, and the refusal is the one that binds. @ `B-10` · _recorded 2026-09-12 13:09 UTC_
- <a id="lead-58"></a>adversarial probe for claim [B-10](subsystems/b10-the-amanuensis-skill.md)/key-type/claude-skills-amanuensis-skill-md-autonomous-phase-execution did not overturn it: the table is present with a row per status, the text states that autonomous execution does not relax it, and the mapped row says explicitly that status alone is not proof a finding survived challenge — so the prose does not claim more for the ladder than the server enforces. @ `B-10` · _recorded 2026-09-12 13:08 UTC_
- <a id="lead-56"></a>adversarial probe for finding [B08-1](findings.md#b08-1) did not find an overturning mechanism: the projection's read-back axes were the candidate, since a divergence between two pages of one publish is what a state axis could catch. The state axis checks the ledger-stale partition and the resolution-marker partition; neither compares the subsystem map's open count against the subsystem pages' resolution states, so the two numbers are never brought together by anything. @ `B-08` · _recorded 2026-09-12 13:03 UTC_
- <a id="lead-55"></a>adversarial probe for finding [B07-1](findings.md#b07-1) did not find an overturning mechanism. Three candidates: a CHECK on file_ledger.stale_reason (the column is plain TEXT, and the vocabulary source records "sql": [] deliberately); a TypeScript union type on the writer (markStale takes the value through a prepared statement's bound parameter, which is typed unknown); and gen-vocabulary.mjs --check-sql (it compares CHECK literals, and this enum binds to no column, so it has nothing to compare). The enum has three copies and no enforcement point. @ `B-07` · _recorded 2026-09-12 13:03 UTC_
- <a id="lead-54"></a>adversarial probe for claim [B-09](subsystems/b09-projection-read-back-and-publication-custody.md)/concurrency did not overturn it, and its honesty is the part that survives: the verification opens no store connection at all, so it genuinely cannot speak to what the store did during the render — the claim understates nothing and overstates nothing. @ `B-09` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-53"></a>adversarial probe for claim [B-09](subsystems/b09-projection-read-back-and-publication-custody.md)/state-container did not overturn it: the two files beside the pages are written once per publish and read by the next verification, and nothing in the module retains them in memory across a call. @ `B-09` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-52"></a>adversarial probe for claim [B-09](subsystems/b09-projection-read-back-and-publication-custody.md)/key-type/materializer-amanuensis-materializer-readback-py-ledger-stale-sections did not overturn it: the three headings map to disjoint classification sets, the partition is checked against OBLIGATION_BEARING_CLASSIFICATIONS rather than against the page, and a heading claiming a classification twice or none is reported by name. @ `B-09` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-51"></a>adversarial probe for claim [B-09](subsystems/b09-projection-read-back-and-publication-custody.md)/key-type/materializer-amanuensis-materializer-readback-py-write-contract did not overturn it: the content axis reads the receipt and hashes the finished files, and nothing in that path re-renders a page to compare against, which is what lets a tree verified at one path be shown to be the tree committed at another. @ `B-09` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-50"></a>adversarial probe for claim [B-08](subsystems/b08-materializer-rendering-pipeline.md)/concurrency did not overturn it: the probe looked for a snapshot mechanism that would make the claim's "cannot see a consistent moment" wrong — a BEGIN, a deferred transaction, immutable=1 — and found none; the URI sets immutable=0 explicitly, which is the opposite choice. @ `B-08` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-49"></a>adversarial probe for claim [B-08](subsystems/b08-materializer-rendering-pipeline.md)/state-container/materializer-amanuensis-materializer-core-py-materializer did not overturn it: the connection is opened inside materialize rather than in __init__ and closed in that function's finally, and no module-level cache survives the call, so a second run re-reads the store rather than a memo of it. @ `B-08` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-48"></a>adversarial probe for claim [B-08](subsystems/b08-materializer-rendering-pipeline.md)/flow/publish/01 did not overturn it: missing_views runs before _plan and its non-empty result returns a refusal, so no page is rendered and nothing is written when a view is absent. @ `B-08` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-47"></a>adversarial probe for claim [B-08](subsystems/b08-materializer-rendering-pipeline.md)/key-type/materializer-amanuensis-materializer-db-py-open-ro did not overturn it: the URI carries mode=ro, which SQLite enforces at the connection rather than by convention, so the process cannot create a view even if a renderer tried — which is what makes the view probe a statement about the store. @ `B-08` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-46"></a>adversarial probe for claim [B-07](subsystems/b07-git-state-staleness-and-refresh.md)/concurrency did not overturn it: every git call in the pass completes before ctx.db.transaction opens, and the transaction contains no subprocess, so the window the claim describes is between the diff and the transaction and nowhere inside it. @ `B-07` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-45"></a>adversarial probe for claim [B-07](subsystems/b07-git-state-staleness-and-refresh.md)/state-container/mcp-server-src-tools-git-ts-gittools did not overturn it: the probe looked for a second writer of the staleness columns and found clear_staleness, which is a tool the coordinator calls rather than a pass, and which lowers the flag without claiming the file was re-examined — so the reconciliation transaction remains the only thing that raises one. @ `B-07` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-44"></a>adversarial probe for claim [B-07](subsystems/b07-git-state-staleness-and-refresh.md)/flow/detect-changes/02 did not overturn it, and found the one asymmetry worth naming: markFresh excludes rows whose reason is 'absent', so a file that returns to the tree is not cleared by this pass. That is deliberate — the row's path is tracked again but nothing re-examined it — and it does not contradict the claim, whose scope is rows that match their examination commit. @ `B-07` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-43"></a>adversarial probe for claim [B-07](subsystems/b07-git-state-staleness-and-refresh.md)/flow/detect-changes/01 did not overturn it: the catch around the per-group diff pushes the whole group to unverifiable and continues, so no row of a failed group reaches the fresh bucket by falling through. @ `B-07` · _recorded 2026-09-12 13:02 UTC_
- <a id="lead-42"></a>adversarial probe for finding [B05-1](findings.md#b05-1) did not find an overturning mechanism: the schema is the other place a guard could live, and it constrains ref_sha to be present and non-empty on neither dispositions nor findings — both columns are plain nullable TEXT — so nothing below the handler asks whether the revision exists. @ `B-05` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-40"></a>adversarial probe for finding [B04-4](findings.md#b04-4) did not find an overturning mechanism: the candidates were a downstream validator and the drift pass. detect_changes reconciles file_ledger.ref_sha and never reads evidence.ref_sha; standing's reachability check reads the ledger row, not the evidence row; and the only code that resolves an evidence row's revision is add_claim, which runs when a claim cites it and not when a disposition or finding does. @ `B-04` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-39"></a>adversarial probe for claim [B-06](subsystems/b06-locus-standing-and-the-reader-lenses.md)/state-container did not overturn it: the probe looked for a memo or cache that would outlive a call — the obvious place being the repeated git probes standing performs — and found the GitProbe built per call, so the cost is paid again each time rather than being retained. @ `B-06` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-38"></a>adversarial probe for claim [B-06](subsystems/b06-locus-standing-and-the-reader-lenses.md)/flow/describe-a-file/02 did not overturn it: the budget is measured on the emitted bytes because the dispatcher honours the tool's compact flag, and the 512-byte locus cap is enforced both in the advertised schema and in boundedSubject, so neither bound rests on a host behaving well. @ `B-06` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-37"></a>adversarial probe for claim [B-06](subsystems/b06-locus-standing-and-the-reader-lenses.md)/flow/describe-a-file/01 did not overturn it: the CASE produces exactly the five states named, in that precedence, and the view is built over file_ledger so a path with no row produces no row at all — which is what leaves unledgered to be inferred rather than selected. The ELSE branch the claim does not mention is reachable only for a classification outside the CHECK, and folds to scoped-unread. @ `B-06` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-36"></a>adversarial probe for claim [B-06](subsystems/b06-locus-standing-and-the-reader-lenses.md)/key-type/mcp-server-src-tools-locus-ts-no-claims-statement did not overturn it: the literal is a module constant emitted in place of the structure section, and the survey prose beside it carries its own not-revision-bound label, so a reader cannot mistake an unrecorded inventory for an empty one. @ `B-06` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-35"></a>adversarial probe for claim [B-06](subsystems/b06-locus-standing-and-the-reader-lenses.md)/key-type/mcp-server-src-standing-ts-standing-authority-order did not overturn it: the order is one exported array read by the resolver, and the mixed case takes the minimum over it rather than a first-wins or last-wins scan, so a file owned by disagreeing subsystems authorizes what its weakest owner does. @ `B-06` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-34"></a>adversarial probe for claim [B-05](subsystems/b05-findings-dispositions-and-resolution-custody.md)/concurrency did not overturn it: the probe looked for a read-modify-write over resolution state that two writers could interleave and found none — every transition reads the prior row only to build the event's rationale, and the insert does not depend on that read being still true. @ `B-05` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-33"></a>adversarial probe for claim [B-05](subsystems/b05-findings-dispositions-and-resolution-custody.md)/flow/read-a-findings-state/01 did not overturn it: both readers in findings.ts join finding_state_current and the verification path reads finding_resolution_current. The probe did find two readers outside this subsystem that do not — list_subsystems and the master-plan renderer — which is [B04-5](findings.md#b04-5) and [B08-1](findings.md#b08-1), not a counterexample to what this subsystem does. @ `B-05` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-32"></a>adversarial probe for claim [B-05](subsystems/b05-findings-dispositions-and-resolution-custody.md)/state-container/mcp-server-src-tools-findings-ts-findingtools did not overturn it: no UPDATE or DELETE touches finding_resolution_events anywhere in the server or the materializer, add_finding inserts the finding and its first event in one transaction, and verify_finding_fix appends rather than amending — with a trigger refusing a verified-fixed event whose evidence is not attached as fix-verification. @ `B-05` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-31"></a>adversarial probe for claim [B-05](subsystems/b05-findings-dispositions-and-resolution-custody.md)/key-type/mcp-server-src-schema-sql-finding-state-current did not overturn it: the view LEFT JOINs finding_resolution_current, so every findings row appears exactly once and the CASE supplies a state for one that never received an event — the claim's "exactly one state" is a property of the join, not of the writers. @ `B-05` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-30"></a>adversarial probe for claim [B-04](subsystems/b04-survey-record-tools.md)/state-container did not overturn it: the probe looked for module-level mutable state across every tool module in this subsystem — a cache, a memo, a lazily built index — and found only frozen constants and prepared statements built per call. @ `B-04` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-29"></a>adversarial probe for claim [B-04](subsystems/b04-survey-record-tools.md)/flow/record-an-edge/03 did not overturn it, including the part most easily wrong: the comment does say "the eighth local copy of the two-line git probe", names findings.ts:37 and claims.ts:104, and states the reason they are not shared — each caller owns a different failure policy. @ `B-04` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-28"></a>adversarial probe for claim [B-04](subsystems/b04-survey-record-tools.md)/flow/record-an-edge/02 did not overturn it: extractWorkspaceCitations throws on the first token whose revision does not resolve, before any citation is returned, so a value carrying one good token and one bad one is refused whole. @ `B-04` · _recorded 2026-09-12 12:57 UTC_
- <a id="lead-27"></a>adversarial probe for claim [B-04](subsystems/b04-survey-record-tools.md)/flow/record-an-edge/01 did not overturn it: add_xref's schema carries the citation pattern built from CITATION_TOKEN_SOURCE, the same exported string the handler's scanner compiles, so the advertised grammar and the enforced one cannot differ. @ `B-04` · _recorded 2026-09-12 12:56 UTC_
- <a id="lead-26"></a>adversarial probe for claim [B-04](subsystems/b04-survey-record-tools.md)/key-type/mcp-server-src-tools-claims-ts-file-anchored-claim-key did not overturn it: the pattern is anchored at the start and matches only key-type, state-container and flow after the subsystem segment, so a key cannot slip the rule by prefixing it, and FILE_ANCHORED_EVIDENCE is a filter over the generated EVIDENCE_KINDS rather than a second literal list. @ `B-04` · _recorded 2026-09-12 12:56 UTC_
- <a id="lead-25"></a>adversarial probe for claim [B-04](subsystems/b04-survey-record-tools.md)/key-type/mcp-server-src-tools-claims-ts-requirefileanchoredevidence did not overturn it: the predicate is a single rows.some over a conjunction, so two rows each satisfying one half cannot pass together, and the refusal string enumerates the kinds and paths it did find. @ `B-04` · _recorded 2026-09-12 12:56 UTC_
- <a id="lead-24"></a>adversarial probe for finding [B03-4](findings.md#b03-4) did not find an overturning mechanism: gen-vocabulary.mjs --check-sql does read schema.sql, which is the one place a check could have noticed, but it extracts CHECK literals and compares value lists — it parses no comment. check-evidence-vocabulary.mjs reads SKILL.md's prose and not the schema's. The stale sentence sits in the gap between them. @ `B-03` · _recorded 2026-09-12 12:51 UTC_
- <a id="lead-23"></a>adversarial probe for finding [B01-1](findings.md#b01-1) did not find an overturning mechanism. Two candidates were checked and both fail for the same reason: check-tool-schemas.mjs imports every dist/tools module and so holds the complete exported set, but asserts schema shape only; test-mcp-compatibility.mjs asserts tools.length &gt; 100 over the served list, a floor rather than an equality. The full set and the served set are both available in this repository and are never compared. @ `B-01` · _recorded 2026-09-12 12:51 UTC_
- <a id="lead-22"></a>adversarial probe for claim [B-03](subsystems/b03-conspectus-schema-vocabulary-and-invariants.md)/concurrency did not overturn it: every invariant helper takes the connection as an argument and is called inside the same synchronous handler as the write it authorizes, and the two that write do so inside db.transaction, so no in-process window exists between a prerequisite passing and the row it admitted. @ `B-03` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-21"></a>adversarial probe for claim [B-03](subsystems/b03-conspectus-schema-vocabulary-and-invariants.md)/state-container did not overturn it, with one qualification worth the record: the negative claim holds — nothing here is written after open — but "frozen set of literals" describes a compile-time guarantee. The generated arrays are `as const`, which is readonly to the type checker and an ordinary mutable array at run time; no code mutates them, so the claim's consequence stands while its word is stronger than the mechanism. @ `B-03` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-20"></a>adversarial probe for claim [B-03](subsystems/b03-conspectus-schema-vocabulary-and-invariants.md)/flow/status-advance/01 did not overturn it: enforcePhasePrerequisites checks the ledger count first and then requireStructuralClaim, and enforceForwardPrerequisites replays every intermediate rung, so an insert opening straight at a later status answers for the same evidence as an advance to it. @ `B-03` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-19"></a>adversarial probe for claim [B-03](subsystems/b03-conspectus-schema-vocabulary-and-invariants.md)/key-type/mcp-server-contracts-conspectus-vocabulary-json-enums-evidence-kind did not overturn it: every value in the source carries label, meaning and cannot_justify, and the file-anchored rule in claims.ts admits exactly code-verified and contract-stated, so the enum states what a value withholds as well as what it grants. @ `B-03` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-18"></a>adversarial probe for claim [B-03](subsystems/b03-conspectus-schema-vocabulary-and-invariants.md)/key-type/mcp-server-src-invariants-ts-enforcemonotonictransition did not overturn it: the guard returns early when either side is deferred, which is the bidirectional exemption the claim states, and throws only when the target rank is strictly lower — so an equal-rank rewrite passes and a regression does not. @ `B-03` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-17"></a>adversarial probe for claim [B-03](subsystems/b03-conspectus-schema-vocabulary-and-invariants.md)/key-type/mcp-server-src-schema-sql-claims did not overturn it: the table declares asserted_at_sha, valid_from_sha and a nullable valid_until_sha with a CHECK refusing an interval that closes where it opens, and supersede_claim closes the predecessor and inserts the successor in one commit rather than updating a row. @ `B-03` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-15"></a>adversarial probe for claim [B-02](subsystems/b02-repository-binding-and-storage-custody.md)/concurrency did not overturn it: the probe looked for a lock that would make the claim's "not provided by this subsystem" wrong and found the write_locks table to be an advisory artifact-path registry tools opt into, never consulted by openDatabase or by any storage write, so nothing here detects a second writer. @ `B-02` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-14"></a>adversarial probe for claim [B-02](subsystems/b02-repository-binding-and-storage-custody.md)/flow/storage-write/01 did not overturn it: resolveStorageOutputPath resolves a relative argument against project.storagePath and hands the result to assertContainedPath with storagePath as the root, so no argument — absolute, traversing, or symlinked — reaches a path outside the storage directory. @ `B-02` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-13"></a>adversarial probe for claim [B-02](subsystems/b02-repository-binding-and-storage-custody.md)/state-container/mcp-server-src-db-ts-opendatabase did not overturn it: openDatabase is the only `new Database(...)` in the server; it sets WAL and foreign_keys, runs migrations strictly before initializeSchema, and closes the connection before throwing when a required view is absent — so a caller never receives a handle to a store that failed the view assertion. @ `B-02` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-12"></a>adversarial probe for claim [B-02](subsystems/b02-repository-binding-and-storage-custody.md)/key-type/mcp-server-src-project-ts-assertcontainedpath did not overturn it: the probe looked for the usual escape — a prefix comparison that a sibling directory sharing a name prefix would satisfy — and found isWithin plus a per-segment lstat and realpath walk, with equality to the root itself also refused. @ `B-02` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-11"></a>adversarial probe for claim [B-02](subsystems/b02-repository-binding-and-storage-custody.md)/key-type/mcp-server-src-project-ts-projectcontext did not overturn it: ProjectContext's members are declared readonly and the binding receipt is built once in resolveProject; no tool handler writes back through the context, so a later call cannot rebind the workspace. @ `B-02` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-10"></a>adversarial probe for claim [B-01](subsystems/b01-server-runtime-and-tool-dispatch.md)/key-type/mcp-server-src-helpers-ts-tooldefinition on its re-asserted reading (B-01-C08) confirms the five-member shape and the (args, ctx) handler order at both the declaration and the dispatch site. @ `B-01` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-9"></a>adversarial probe for claim [B-01](subsystems/b01-server-runtime-and-tool-dispatch.md)/concurrency did not overturn it: the probe looked for an async boundary inside a handler that could interleave two calls against the connection and found none — better-sqlite3 is synchronous, the transport is one stdio stream, and the dispatcher awaits nothing between validation and the handler's return. @ `B-01` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-8"></a>adversarial probe for claim [B-01](subsystems/b01-server-runtime-and-tool-dispatch.md)/flow/tool-call/02 did not overturn it: the validators Map is compiled once at startup from each tool's own inputSchema with Ajv strict:true, and the call path looks the validator up before invoking the handler, so a malformed schema fails at startup and a mistyped argument fails before any handler runs. @ `B-01` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-7"></a>adversarial probe for claim [B-01](subsystems/b01-server-runtime-and-tool-dispatch.md)/flow/tool-call/01 did not overturn it: allTools is one concatenation with locusTools first, and both the advertised list and the byName dispatch Map are built from that same array, so an ordering or membership difference between what is advertised and what is dispatchable is not expressible here. @ `B-01` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-6"></a>adversarial probe for claim [B-01](subsystems/b01-server-runtime-and-tool-dispatch.md)/state-container/mcp-server-src-index-ts-main did not overturn it: `let db: DB | null` in main, an ensureDatabase closure that returns the existing handle when one is set, and a getter on the context are the whole of it; no path reopens or clears the binding, so the stale-handle consequence the claim states is the one the code has. @ `B-01` · _recorded 2026-09-12 12:50 UTC_
- <a id="lead-5"></a>adversarial probe for claim [B-01](subsystems/b01-server-runtime-and-tool-dispatch.md)/key-type/mcp-server-src-index-ts-toolannotations did not overturn it: readOnlyHint is READ_ONLY_TOOLS membership or a get_/list_/lookup_ prefix, destructiveHint is the negation of that conjoined with ADDITIVE_TOOLS membership, and idempotentHint tracks readOnly — so destructive is the default for an unprefixed, unlisted tool, exactly as recorded. @ `B-01` · _recorded 2026-09-12 12:49 UTC_
- <a id="lead-4"></a>adversarial probe for claim [B-01](subsystems/b01-server-runtime-and-tool-dispatch.md)/key-type/mcp-server-src-helpers-ts-servercontext did not overturn it: ServerContext declares exactly project, db and sessionId, and index.ts builds the single instance with db as a getter over ensureDatabase, so no handler reaches the store or the workspace through module state. @ `B-01` · _recorded 2026-09-12 12:49 UTC_
- <a id="lead-1"></a>The two-line `git rev-parse --verify <rev>^{commit}` probe exists in at least eight copies across the tool modules, and the comment at xrefs.ts:21-36 says so explicitly and declines to share them, because each caller owns a different failure policy. Worth knowing before anyone proposes the obvious refactor: the duplication is a recorded decision, not drift. @ `mcp-server/src/tools/xrefs.ts:resolvesInWorkspace@258ccd2` · _recorded 2026-09-12 12:18 UTC_

